# Rune Chess Match-3 upgrade suggestions

## 2026-06-13 Portrait combat skeleton follow-ups

- [P0][2h] Replace the HUD value tiles with real widgets: a run-HP bar+number, a gold tile with an icon glyph, and a player level + XP progress bar, all token-sized.
- [P0][2h] Once the no-shop/no-bench hero-acquisition mechanic is specified, build the prep-phase bottom panel for it (mirroring the combat skeleton so prep and combat share one layout shell).
- [P1][2h] Extract the per-zone heights (HUD/arena/board/status/nav) into named layout constants so the 844px portrait budget is verifiable in one place and resists drift.
- [P1][1h] Add a win/loss streak indicator to the combat HUD fed from run state, completing the top-bar spec in docs/visual-style.md.
- [P2][2h] Introduce a lightweight headless layout-budget check (sum of zone heights ≤ content height) runnable in CoreSmoke to catch portrait overflow without Unity.

## 2026-06-13 Auto-battler visual overhaul foundation

- [P0][3h] Build the portrait combat skeleton in `PortraitGameBootstrap` from `docs/visual-style.md`: vertical stack of HUD → tactical arena → match-3 board → bottom panel, all sized off `UiTheme` spacing tokens.
- [P0][2h] Add rarity-framed shop cards (portrait + cost gem + `GameColors.RarityColor` border) and route shop offers through them, replacing the current primitive rows.
- [P0][2h] Wire the slide-out alliance panel to `SynergyPanelModel` and colour each row with `GameColors.SynergyTierColor`, including the beginner spotlight.
- [P1][3h] Generate the original placeholder sprite set (unit, rune glyphs, rarity frames, faction/class icons) behind the token API so views need no art changes when finals land.
- [P1][2h] Add a CoreSmoke check that asserts `GameColors` maps every `HeroRarity`/`RuneType`/`SynergyStrength` without throwing, guarding token/enum drift between core and presentation.

## 2026-06-12 CoreSmoke CI automation

- [P1][2h] Add a status badge for the `Core Smoke` workflow to `README.md` so the verification state is visible at a glance.
- [P1][2h] Fix the three `CS8602` nullable warnings in `tools/CoreSmoke/Program.cs` and enable `-warnaserror` in CI so new core regressions surface as build failures.
- [P1][3h] Split the 3.6k-line `tools/CoreSmoke/Program.cs` into named check groups (run loop, combat, match-3, monetization, PvP) for readable CI output and faster failure localization.
- [P1][3h] Add a `.NET`-based timing assertion or lightweight benchmark step to CI that flags if a full CoreSmoke run regresses past a threshold, guarding determinism cost.
- [P2][4h] Investigate a Unity edit-mode test job (e.g. GameCI) so presentation-layer tasks gain the same automated verification the core package now has.

- [P0][2h] Open the repository in Unity 6.3 LTS on Windows, generate `Assets/Scenes/Main.unity`, and commit the generated scene.
- [P0][3h] Expand `RunState` with typed team, bench, shop, commander, artifacts, XP, and gold command handling.
- [P0][4h] Complete the pure C# match-3 engine with gravity, refill, chain resolution, and matchPower calculation.
- [P0][3h] Add Unity edit-mode tests for match-3, run progression, and hero data validation.
- [P0][3h] Replace the generated uGUI sample cells with reusable tactical field and rune board view components.

## 2026-05-22 Windows-first migration

- [P0][3h] Add ScriptableObject importers for hero, commander, rune, synergy, rarity, and round content.
- [P0][3h] Add a combat core module with attack timers, targeting, mana gain, and win/loss formula tests.
- [P0][2h] Add a portrait Game view preset checklist for iPhone SE and standard iPhone dimensions.
- [P0][2h] Add placeholder sprite assets for six rune colors and tactical unit markers.
- [P1][4h] Configure Unity Build Automation or Codemagic for signed iOS cloud builds once Apple credentials are ready.

## 2026-05-22 central run state

- [P0][2h] Install a pinned .NET SDK or Unity Editor CI image so `tools/CoreSmoke` compiles in automation.
- [P0][3h] Add Unity edit-mode tests for `RunState.BuyHero`, `PlaceHeroFromBench`, `BuyXp`, `RerollShop`, and phase guards.
- [P0][3h] Introduce typed shop-generation odds by player level instead of passing reroll offers manually.
- [P0][2h] Add stable save/load DTOs for `RunState` before adding between-screen persistence.
- [P0][3h] Create content fixtures for the first commander, first three heroes, and round-one enemy preview.

## 2026-05-22 core loop slice

- [P0][3h] Add typed `RoundDefinition` data for enemy preview, reward gold, event routing, and combat seed per MVP round.
- [P0][4h] Expand `CombatState` from rune swaps into combat outcome tracking with enemy health summaries and player damage.
- [P0][3h] Add a deterministic shop refresh service so `AdvanceRound` can generate offers from level-based odds.
- [P0][2h] Show `RunState.Phase` and `CombatState.LastMatchPower` in the portrait action panel for manual Unity validation.
- [P0][3h] Add CoreSmoke cases for invalid rune swaps, negative rewards, and reward/advance phase guards once .NET SDK is installed.

## 2026-05-22 pve round schedule

- [P0][3h] Replace hardcoded schedule entries with JSON or ScriptableObject round content imported into core fixtures.
- [P0][2h] Add per-round tags for normal, elite, event, and boss encounters before reward routing expands.
- [P0][3h] Add enemy squad templates to `PveRoundDefinition` so combat can spawn round-specific opponents.
- [P0][2h] Expose current round reward and next enemy name in the Unity portrait header.
- [P0][4h] Add deterministic replay smoke coverage that records the full 10-round phase sequence.

## 2026-05-22 final victory state

- [P0][2h] Add a run summary DTO that captures victory, final round, remaining health, gold, team, and artifacts.
- [P0][3h] Add portrait victory screen presentation wired to `RunState.IsRunWon`.
- [P0][2h] Add a post-victory guard that blocks preparation, shop, combat, and reward commands after run completion.
- [P0][2h] Add victory reward hooks for meta XP and soft currency without affecting combat balance.
- [P0][3h] Add Unity play-mode smoke that reaches the victory phase through the generated portrait surface.

## 2026-05-22 run defeat state

- [P0][2h] Replace raw defeat reason strings with a typed `DefeatReason` enum or value object.
- [P0][3h] Add round-loss resolution that subtracts run health but continues the PvE run when health remains.
- [P0][3h] Wire all-allies-defeated, timer loss, and boss-condition failures into `ResolveCombatDefeat`.
- [P0][2h] Add portrait defeat summary UI that displays the recorded defeat reason and final squad.
- [P0][3h] Add command guards that reject shop, XP, reroll, combat, and reward actions after defeat.

## 2026-05-24 combat timer outcomes

- [P0][3h] Replace health-percent tick inputs with typed combat team summaries once unit health exists.
- [P0][2h] Surface `CombatState.RemainingSeconds` in the portrait combat action panel.
- [P0][3h] Add timer-based run health damage after round losses instead of immediate run defeat.
- [P0][2h] Add per-round combat duration overrides to `PveRoundDefinition` for tutorial and boss pacing.
- [P0][3h] Add deterministic smoke replay coverage for timer victory, timer defeat, and simultaneous lethal outcomes.

## 2026-05-24 run progress snapshots

- [P0][3h] Add a Unity `ScriptableObject` or service wrapper that owns `RunProgressStore` across screen prefabs.
- [P0][3h] Add JSON serialization tests for `RunProgressSnapshot` once the target Unity serializer is chosen.
- [P0][2h] Add snapshot validation for impossible phase, round, health, and economy combinations.
- [P0][3h] Persist progress to `Application.persistentDataPath` for app suspend/resume recovery.
- [P0][2h] Add a migration path for future `RunProgressSnapshot.CurrentVersion` increments.

## 2026-05-24 tactical field model

- [P0][2h] Add explicit `TacticalSide` helpers for player and enemy halves of the MVP field.
- [P0][3h] Move tactical cell coloring in Unity presentation to `TacticalField` side metadata.
- [P0][2h] Add field-shape smoke coverage for every boundary cell and invalid negative coordinates.
- [P0][3h] Add enemy spawn positions to `PveRoundDefinition` using `TacticalPosition`.
- [P0][2h] Add a visual debug overlay that prints tactical row/column coordinates in the Unity scene.

## 2026-05-24 tactical field sides

- [P0][2h] Add row labels for frontline and backline within each tactical side.
- [P0][3h] Add enemy-side placement helpers for round templates and combat spawn setup.
- [P0][2h] Add Unity cell state colors from `TacticalSide` instead of presentation-local booleans.
- [P0][3h] Add validation that odd-row tactical fields cannot be used for equal-side PvE layouts.
- [P0][2h] Add smoke coverage for `TacticalPosition.IsEnemySide` and `IsPlayerSide` on invalid cells.

## 2026-05-24 player-side placement guard

- [P0][2h] Add UI disabled states for enemy-side cells during preparation.
- [P0][3h] Add drag-and-drop feedback that previews legal and illegal placement cells.
- [P0][2h] Add a typed placement result object so UI can show precise rejection reasons.
- [P0][3h] Add edit-mode tests for bench-to-field placement guards once Unity Test Framework is available.
- [P0][2h] Add tutorial hints that highlight the player half before the first placement.

## 2026-05-24 bench field movement

- [P0][3h] Add drag source/target adapters that call `PlaceHeroFromBench` and `MoveHeroToBench`.
- [P0][2h] Add bench full feedback when field-to-bench movement is rejected.
- [P0][3h] Add swap-position support for moving a fielded hero onto another occupied player cell.
- [P0][2h] Add smoke coverage for invalid hero instance IDs in both movement directions.
- [P0][3h] Add Unity play-mode coverage for bench slot and field cell interaction.

## 2026-05-24 field hero cap

- [P0][2h] Show fielded hero count as `team/playerLevel` in the preparation action panel.
- [P0][3h] Add level-up threshold data so player level changes come from XP progression rules.
- [P0][2h] Add placement rejection feedback when the field cap is reached.
- [P0][3h] Add smoke coverage for cap changes after `LevelUp`.
- [P0][2h] Add a tutorial step that explains why extra heroes remain on the bench.

## 2026-05-24 next enemy preview

- [P0][3h] Replace raw enemy IDs in the preparation UI with data-driven display names and threat tags.
- [P0][2h] Add enemy preview stat chips for frontline count, backline threat, and rune affinity.
- [P0][3h] Add `EnemySquadPreview` data to `PveRoundDefinition`.
- [P0][2h] Add a compact portrait tooltip panel for next-enemy details.
- [P0][3h] Add Unity smoke coverage that verifies the preparation header renders the next enemy preview.

## 2026-05-24 tactical lines

- [P0][3h] Add melee targeting preference for enemy frontline before backline.
- [P0][3h] Add ranged targeting rules that can reach backline based on attack type.
- [P0][2h] Add front/back row labels to the tactical field debug overlay.
- [P0][3h] Add line-based hero role validation hints for tanks, carries, and healers.
- [P0][2h] Add smoke coverage for future 7x4 field line semantics if the MVP field changes.

## 2026-05-24 tactical cell states

- [P0][3h] Drive tactical cell states from `RunState.Team` and enemy preview data instead of demo coordinates.
- [P0][2h] Add selected-hero placement preview overlays for available and unavailable cells.
- [P0][3h] Add Unity play-mode assertions for every tactical cell visual state.
- [P0][2h] Add accessible shape markers alongside color differences for occupied and available cells.
- [P0][3h] Add animation hooks for cell state transitions during drag, placement, and combat start.

## 2026-05-24 match-3 board 7x7

- [P0][2h] Add Unity presentation binding that renders all 49 `Match3Board` cells in a portrait-safe square grid.
- [P0][2h] Add board seed fixtures that guarantee at least one legal opening swap for smoke and tutorial flows.
- [P0][3h] Add edit-mode tests for `Match3Board.Contains`, `CreateCells`, constructor sizing, and boundary index access.
- [P0][2h] Add coordinate labels to the rune-board debug overlay for quick manual validation in Unity.
- [P0][3h] Add a board serialization DTO so saved combat progress is independent of runtime class internals.

## 2026-05-24 rune type catalog

- [P0][2h] Add localized display names and compact labels for every canonical rune id.
- [P0][2h] Move rune presentation colors to data keyed by `RuneTypes.GetId` instead of enum switches.
- [P0][3h] Add edit-mode tests for rune id parsing, unknown ids, and data-import round trips.
- [P0][3h] Add rune effect descriptors that map each rune type to its MVP combat role.
- [P0][2h] Add a debug inspector panel that lists rune ids, colors, and planned effects from one catalog.

## 2026-05-24 adjacent rune swap

- [P0][3h] Wire portrait rune-tile drag input to `Match3Board.CanSwap` before dispatching a combat command.
- [P0][2h] Add invalid-swap UI feedback for diagonal, same-cell, and out-of-board attempts.
- [P0][3h] Add deterministic edit-mode tests for swap immutability and coordinate boundary cases.
- [P0][2h] Add a short swap animation contract so presentation can animate the two exchanged runes.
- [P0][3h] Split raw board swap from legal combat move resolution so invalid no-match swaps can animate and revert cleanly.

## 2026-05-24 legal match swaps

- [P0][3h] Add a typed match result object that reports matched cells caused by a legal swap.
- [P0][2h] Add UI preview states for legal, adjacent-but-no-match, and invalid rune drags.
- [P0][3h] Add deterministic fixtures that cover legal swaps with horizontal and vertical created matches.
- [P0][2h] Add combat log entries for rejected no-match swaps during manual testing.
- [P0][3h] Separate existing-board cleanup from player swap validation before gravity and chains are implemented.

## 2026-05-24 horizontal vertical match scan

- [P0][3h] Add match group metadata with direction, rune type, length, and involved cells.
- [P0][2h] Add debug visualization that outlines horizontal and vertical matches separately.
- [P0][3h] Add edit-mode tests for match-4, match-5, and board-edge match detection.
- [P0][3h] Preserve match direction metadata through rune removal and effect resolution.
- [P0][2h] Add a compact combat log entry that reports matched rune color and match length.

## 2026-05-24 matched rune removal

- [P0][3h] Add a match-resolution DTO that carries removed cells, rune color, and match length into effects.
- [P0][3h] Add gravity tests that verify empty cells collapse upward columns without losing rune order.
- [P0][2h] Add presentation support for empty rune cells during removal and falling animations.
- [P0][3h] Update progress snapshots to reject or explicitly serialize transient empty-board states.
- [P0][2h] Add removal animation timing data so Unity can delay refill until matched runes disappear.

## 2026-05-24 rune drop refill

- [P0][3h] Add a `BoardDropResult` DTO with moved runes and newly spawned cells for animation.
- [P0][2h] Surface deterministic refill seeds in combat logs for replay debugging.
- [P0][3h] Add edit-mode tests for multi-column drops, full-column clears, and no-empty no-op drops.
- [P0][3h] Update progress snapshots to allow only stable refilled boards between screens.
- [P0][2h] Add portrait rune-fall animation timing that mirrors `DropRunesFromTop` column movement.

## 2026-05-24 chain reactions

- [P0][3h] Add a per-step match effect queue that consumes `Match3ChainResolution` in combat.
- [P0][2h] Expose chain step metadata to Unity so removal, falling, and refill animations can play sequentially.
- [P0][3h] Add deterministic replay snapshots that include refill seed and every chain step.
- [P0][2h] Add combat log entries for chain number, combo depth, and matched rune count.
- [P0][3h] Add edit-mode fixtures for longer chain 3 and chain 4+ bonus scenarios.

## 2026-05-24 match power scoring

- [P0][3h] Emit per-step matchPower events so rune effects can consume exact chain scoring.
- [P0][2h] Show matchPower in a temporary combat debug overlay for balance tuning.
- [P0][3h] Add effect tests that compare base matchPower, combo-depth offsets, and chain totals.
- [P0][2h] Add a small balancing table for future matchPower modifiers such as T/L combos.
- [P0][3h] Persist last matchPower breakdown in combat snapshots for replay and debugging.

## 2026-05-24 swap global cooldown

- [P0][2h] Add a UI cooldown ring or disabled drag state for the 0.25s post-swap lockout.
- [P0][3h] Route combat input through a command validator that reports cooldown-specific rejection reasons.
- [P0][2h] Add millisecond combat tick plumbing so cooldowns and timers can share one clock.
- [P0][3h] Add edit-mode tests for cooldown persistence across save/load and scene changes.
- [P0][2h] Add tuning config for swap cooldown instead of keeping the MVP value as a constant.

## 2026-05-24 idle match hints

- [P0][3h] Bind `Match3MoveHint.HighlightedCells` to Unity rune-tile highlight states.
- [P0][2h] Add a subtle pulse animation that starts only after the eight-second idle delay.
- [P0][3h] Track player hint acceptance so future tutorials can adapt hint frequency.
- [P0][2h] Add config for hint delay and disable hints during scripted tutorial prompts.
- [P0][3h] Add deterministic hint fixtures for boards with multiple legal swaps and no legal swaps.

## 2026-05-24 large combo slowdown

- [P0][3h] Bind `CombatState.CombatSpeedPercent` into the autobattle simulation tick rate.
- [P0][2h] Add a brief screen-space timing cue when slowdown starts and expires.
- [P0][3h] Move large-combo thresholds into balance config for match-4, chain, and T/L tuning.
- [P0][2h] Add telemetry counters for slowdown triggers per combat round.
- [P0][3h] Add animation tests that verify cooldown, hint, and slowdown timers can overlap cleanly.

## 2026-06-01 rune effect resolution

- [P0][4h] Wire `RuneEffectResolver` output into the autobattle layer so resolved effects actually apply damage, mana, healing, and shields to units.
- [P0][3h] Add great-rune board persistence: place a great rune on a Match-5, mark its cell, and apply the x2.5 multiplier when it is matched again.
- [P0][2h] Track the once-per-round chain-4+ gold bonus on `RunState`/`CombatState` and grant it during reward resolution.
- [P0][3h] Resolve per-chain-step effects from `Match3ChainResolution` by recording each step's pre-removal board so colors and shapes survive the swap pipeline.
- [P0][2h] Move rune-effect constants (chain multipliers, T/L bonus, great-rune multiplier, color-to-kind map) into a balance config asset for tuning without code changes.

## 2026-06-01 combat formulas

- [P0][4h] Build a `CombatUnit` model (stats, current health/mana/shield, stars) that consumes `CombatFormulas` for an autobattle tick loop.
- [P0][3h] Add a deterministic crit RNG stream per combat seed so `WouldCrit` rolls are reproducible across save/load.
- [P0][2h] Move combat constants (defense softening, mana gains, crit, shield cap) into a balance config asset for tuning without code changes.
- [P0][3h] Connect `RuneEffectResolver` outputs to `CombatFormulas` so red runes apply physical damage, green runes heal, yellow runes add capped shields, and blue runes grant mana.
- [P0][2h] Add synergy/artifact multiplier providers so `CalculateFinalHealth` and damage formulas can read active run modifiers.

## 2026-06-01 hero data model

- [P0][4h] Author the first 20 heroes as `HeroDefinition` data with per-rarity stats and link them to `ShopOffer` HeroIds.
- [P0][3h] Drive shop offer costs from `HeroRarities.GetCostRange` and level-based rarity odds instead of hardcoded offer costs.
- [P0][3h] Add a hero ability/passive model (effect type, magnitude, trigger) so abilities are data-driven instead of description strings.
- [P0][3h] Implement hero merging (3x1★→2★, 3x2★→3★, optional 2x1★→2★) and selling on `RunState` with the economy rules.
- [P0][2h] Add a hero catalog lookup service so `HeroInstance.HeroId` resolves to its `HeroDefinition` and scaled stats.

## 2026-06-01 autobattle core

- [P0][4h] Wire `RuneEffectResolver` effects into `BattleState` so red runes deal damage, green heal, yellow add capped shields, and blue grant mana through one combat command.
- [P0][3h] Add a deterministic crit RNG stream to `BattleState.Tick` using a battle seed so `CombatFormulas.WouldCrit` rolls are reproducible.
- [P0][3h] Implement data-driven hero abilities triggered by `AbilitiesCast` (damage, heal, shield, summon) instead of only counting casts.
- [P0][3h] Connect `BattleState` outcome to `RunState.ResolveCombatTick` (player/enemy health percent, survivors) so PvE rounds drive run health and rewards.
- [P0][2h] Add magic-damage attacks and per-unit attack-type targeting rules (assassins to backline) on top of the nearest-enemy default.

## 2026-06-01 rune effects in combat

- [P0][3h] Call `BattleState.ApplyRuneEffects` from the run/combat controller after each match-3 swap, feeding `RuneEffectResolver.ResolveStep` output per chain step.
- [P0][3h] Implement great-rune board persistence and activation so `IsGreatRuneActivation` effects (x2.5) actually fire from the board.
- [P0][2h] Spend accumulated `BattleState.CommanderEnergy` on a commander ultimate once it reaches the commander's max energy.
- [P0][3h] Add per-color targeting rules from the GDD (e.g. red hits a random enemy via a battle RNG seed instead of the lowest-health focus default).
- [P0][2h] Tune rune effect `Power`-to-stat scaling (damage/heal/shield/mana) against the 45-75s combat and 4-8 swap balance targets.

## 2026-06-01 match-5 great rune creation

- [P0][est: 1100 tokens] Activate `RuneCell.IsGreatRune` when a later match includes the stored great-rune cell.
- [P0][est: 900 tokens] Apply the x2.5 great-rune multiplier through `RuneEffectResolver.ResolveStep` using board cell metadata.
- [P0][est: 800 tokens] Expose `Match3ChainStep.CreatedGreatRunes` to Unity presentation for spawn animation timing.
- [P0][est: 700 tokens] Add a portrait rune-tile visual marker for great runes that preserves color readability.
- [P1][est: 1000 tokens] Add backward-compatible progress snapshot migration for saves created before great-rune flags.

## 2026-06-01 great rune activation

- [P0][est: 900 tokens] Surface `RuneEffect.IsGreatRuneActivation` in combat logs for balance debugging.
- [P0][est: 1100 tokens] Add Unity rune activation animation that distinguishes creation from detonation.
- [P0][est: 800 tokens] Add resolver coverage for simultaneous great-rune activations in separate match groups.
- [P0][est: 1000 tokens] Tune great-rune x2.5 scaling against red damage, green healing, and yellow shield caps.
- [P1][est: 700 tokens] Add analytics counters for created, activated, and expired great-rune opportunities.

## 2026-06-01 chain gold reward

- [P0][est: 900 tokens] Add a reward-screen line item that explains the chain 4+ bonus gold.
- [P0][est: 800 tokens] Expose `EarnedChainFourGoldBonus` in combat debug UI for manual validation.
- [P0][est: 1200 tokens] Add deterministic match-3 fixtures that naturally produce chain 4+ from a legal swap.
- [P0][est: 900 tokens] Tune chain bonus frequency against the 4-8 swaps per combat target.
- [P1][est: 700 tokens] Track chain bonus earned/missed counters for future onboarding hints.

## 2026-06-01 active hero abilities

- [P0][est: 1400 tokens] Replace role-derived active abilities with explicit per-hero ability data from the hero catalog.
- [P0][est: 1100 tokens] Add active ability targeting rules for nearest enemy, lowest-health ally, and front-line ally.
- [P0][est: 1000 tokens] Surface ability casts in the portrait combat log and unit status chips.
- [P0][est: 1200 tokens] Add deterministic ability tests for enemy-side casts and lethal ability outcomes.
- [P1][est: 900 tokens] Add ability cooldown and cast animation hooks separate from attack cooldowns.

## 2026-06-01 passive hero effects

- [P0][est: 1300 tokens] Replace role-derived passive effects with explicit per-hero passive data from the hero catalog.
- [P0][est: 1100 tokens] Add battle-start passive application for team-wide buffs and aura effects.
- [P0][est: 1000 tokens] Add combat-event passive triggers for on-hit, on-cast, on-kill, and on-shield events.
- [P0][est: 900 tokens] Surface passive modifiers in hero detail and combat debug UI.
- [P1][est: 800 tokens] Add balance tests that compare passive role archetypes across star levels.

## 2026-06-01 one-star hero merge

- [P0][est: 900 tokens] Add automatic merge checks after hero purchase and reward grants.
- [P0][est: 1000 tokens] Add merge preview UI for bench and board copies before consuming heroes.
- [P0][est: 800 tokens] Preserve field position and user-selected survivor priority when multiple placed copies can merge.
- [P0][est: 900 tokens] Add progress snapshot coverage for merged hero stars across save and restore.
- [P1][est: 700 tokens] Add merge telemetry counters for one-star upgrades and blocked merge attempts.

## 2026-06-01 two-star hero merge

- [P0][est: 900 tokens] Add automatic chained merging when a new 2-star copy completes a 3-star upgrade.
- [P0][est: 1000 tokens] Add UI treatment for 3-star creation with board/bench survivor highlighting.
- [P0][est: 800 tokens] Add merge validation for mixed bench and field 2-star copies.
- [P0][est: 900 tokens] Prevent excess copies from being consumed when several duplicate 2-star sets exist.
- [P1][est: 700 tokens] Add balance telemetry for time-to-first-3-star across MVP rounds.

## 2026-06-01 early two-copy merge option

- [P0][est: 900 tokens] Move the early merge copy count into a named test/balance config object.
- [P0][est: 800 tokens] Add a visible dev-toggle label when two-copy one-star merging is enabled.
- [P0][est: 900 tokens] Prevent production builds from accidentally enabling the early merge shortcut.
- [P0][est: 1000 tokens] Add scenario tests for chained early merges after shop purchases.
- [P1][est: 700 tokens] Track early-merge usage separately from standard three-copy upgrades.

## 2026-06-01 hero selling economy

- [P0][est: 900 tokens] Resolve `SellHero` base cost through a hero catalog instead of requiring the caller to pass it.
- [P0][est: 800 tokens] Add UI confirmation for selling upgraded heroes with visible refund value.
- [P0][est: 1000 tokens] Track invested copy count separately when early two-copy merge mode is enabled.
- [P0][est: 900 tokens] Add sell-button availability states for bench, board, combat, and full-economy edge cases.
- [P1][est: 700 tokens] Add telemetry for hero sale value, star level, and phase-blocked sale attempts.

## 2026-06-01 iron guard catalog entry

- [P0][est: 1000 tokens] Move hero catalog entries into data assets or JSON once the MVP roster is complete.
- [P0][est: 900 tokens] Add localized display text keys for hero names, abilities, and passives.
- [P0][est: 800 tokens] Add catalog validation that every shop offer references an existing hero id.
- [P0][est: 1000 tokens] Add per-star stat overrides when the project moves beyond formula-only star scaling.
- [P1][est: 700 tokens] Add a hero catalog diff report for balance-review changes.

## 2026-06-01 oath archer catalog entry

- [P0][est: 900 tokens] Add role-based stat baselines for common carry heroes so future entries stay consistent.
- [P0][est: 900 tokens] Add explicit active-ability data for multi-shot carry abilities instead of description strings.
- [P0][est: 800 tokens] Add ranged targeting validation for current-target and nearest-target catalog values.
- [P0][est: 900 tokens] Add shop-offer catalog validation once `field_medic` is present.
- [P1][est: 700 tokens] Add balance snapshots comparing common tank, carry, and healer stat curves.

## 2026-06-01 field medic catalog entry

- [P0][est: 900 tokens] Replace healer targeting strings with a typed target enum for lowest-health ally behavior.
- [P0][est: 1000 tokens] Add explicit healer active-ability data with heal amount and target count.
- [P0][est: 800 tokens] Add catalog validation that green-rune healers map to healing rune effects.
- [P0][est: 900 tokens] Add shop smoke tests for buying every starting-shop catalog hero.
- [P1][est: 700 tokens] Add balance snapshots for common healer survivability and mana cadence.

## 2026-06-01 wild claw catalog entry

- [P0][est: 900 tokens] Add typed class ids so `Берсерк` heroes can be grouped without raw string matching.
- [P0][est: 1000 tokens] Implement bruiser-specific active data for attack speed and lifesteal effects.
- [P0][est: 800 tokens] Add catalog validation that red bruisers map to physical damage rune affinities.
- [P0][est: 900 tokens] Add Wild faction baseline stats for future common and rare heroes.
- [P1][est: 700 tokens] Add balance snapshots for bruiser health-to-damage ratios.

## 2026-06-01 thorn shaman catalog entry

- [P0][est: 1000 tokens] Add summon-unit definitions for temporary beasts created by summoner heroes.
- [P0][est: 900 tokens] Replace `summon_slot` targeting strings with typed summon target rules.
- [P0][est: 800 tokens] Add catalog validation that summoner passives map to opening mana effects.
- [P0][est: 900 tokens] Add Wild faction stat baselines for support-style heroes.
- [P1][est: 700 tokens] Add balance snapshots for summon mana cadence and summon uptime.

## 2026-06-02 remaining MVP roster (15 heroes)

- [P0][est: 1200 tokens] Replace free-form `Targeting` strings (`farthest_enemy`, `two_targets`, `backline_enemy`, `all_allies`, `battlefield`) with a typed targeting enum so combat resolution can branch safely.
- [P0][est: 1000 tokens] Add explicit per-ability data (jump-crit, multi-target magic missile, team-heal pulses, time-stop) instead of description strings so the auto-battler can execute them.
- [P0][est: 900 tokens] Add catalog stat-curve validation that asserts cost tier vs. base health/attack ranges to keep future heroes balanced.
- [P0][est: 900 tokens] Add a roster coverage test that every faction has at least one front-line and one back-line role for synergy bring-up.
- [P1][est: 800 tokens] Add the `legendary_caster` GDD role label as a derived tag so `astral_regent` can be queried without overloading `HeroRole.Caster`.

## 2026-06-02 faction/class synergy data foundation

- [P0][est: 1400 tokens] Wire active synergies from `SynergyCalculator` into `BattleState` setup so the tier effects (armor, attack speed, shields, debuffs) actually apply in combat.
- [P0][est: 1000 tokens] Add the four match-3-triggered synergy effects (Empire-4 shield, Wild-4 lifesteal, Abyssal-4 purple bonus, Mechanist-4 turret) to the rune-resolution pipeline.
- [P0][est: 900 tokens] Replace the Russian-string faction/class keys on `HeroDefinition` with the typed `SynergyDefinition` ids to remove stringly-typed grouping.
- [P1][est: 800 tokens] Expose a `RunState.ActiveSynergies` helper so prep/battle UI panels can read synergy progress without recomputing.
- [P1][est: 700 tokens] Add class synergy breakpoints (or an explicit "no synergy" rationale) for Стрелок, Целитель, Призыватель and Берсерк to finish class parity.

## 2026-06-02 empire armor synergy

- [P0][est: 1000 tokens] Add a battle setup factory that converts `RunState.Team` into synergy-buffed player `BattleUnit`s in one place.
- [P0][est: 900 tokens] Surface active stat modifiers in the preparation synergy panel so players can see the +10% Empire armor before combat.
- [P0][est: 900 tokens] Add per-round enemy battle-unit creation with separate enemy synergy modifiers for future mirrored encounters.
- [P0][est: 800 tokens] Add smoke coverage for stacked stat synergies once Wild attack speed and Defender health modifiers are implemented.
- [P1][est: 700 tokens] Add a roster-distribution validator that checks the GDD table, catalog data, and task summary agree on faction counts.

## 2026-06-02 empire frontline shield

- [P0][est: 900 tokens] Add combat-log entries that show when Empire 4 expands a yellow-rune shield to the frontline.
- [P0][est: 800 tokens] Add a portrait frontline shield animation cue that highlights only affected tactical cells.
- [P0][est: 1000 tokens] Tune yellow-rune shield power against the 60% shield cap when Empire 4 hits two or more units.
- [P0][est: 900 tokens] Add enemy-side coverage for future PvE encounters that can activate Empire-like frontline shield effects.
- [P1][est: 700 tokens] Add tooltip copy that explains Empire 2 vs. Empire 4 without requiring players to read raw tier text.

## 2026-06-02 wild attack speed synergy

- [P0][est: 900 tokens] Add UI stat chips that show attack-speed changes from Wild 2 on each affected unit.
- [P0][est: 900 tokens] Add combat telemetry for attacks-per-second before and after active synergy modifiers.
- [P0][est: 1000 tokens] Add stacked speed tests once hero passives or artifacts can also modify attack speed.
- [P0][est: 900 tokens] Tune Wild 2 against the 45-75 second battle-duration target and 4-8 rune swaps per combat.
- [P1][est: 700 tokens] Add a balance report comparing Wild frontline bruisers against Empire defensive openings.

## 2026-06-02 wild chain lifesteal synergy

- [P0][est: 1000 tokens] Add combat-log events for Wild 4 lifesteal start, refresh, healing amount, and expiry.
- [P0][est: 900 tokens] Add a small unit status icon for temporary lifesteal with remaining duration.
- [P0][est: 900 tokens] Tune Wild 4 lifesteal fraction and duration against chain frequency in deterministic combat fixtures.
- [P0][est: 1000 tokens] Add coverage for lifesteal interactions with shields, anti-healing, and ability damage once those sources can trigger it.
- [P1][est: 700 tokens] Add a tutorial hint that explains why chain reactions are especially valuable for Wild teams.

## 2026-06-02 abyssal weakness synergy

- [P0][est: 1000 tokens] Add combat-log events for Abyssal 2 weakness application, refresh, affected target, and expiry.
- [P0][est: 900 tokens] Add a small unit status icon for temporary weakness with remaining duration and attack penalty.
- [P0][est: 1000 tokens] Add typed status-effect data so weakness, lifesteal, and future debuffs do not expand `BattleUnit` with one-off fields.
- [P0][est: 900 tokens] Tune Abyssal 2 weakness penalty and duration against caster/support mana cadence in deterministic fixtures.
- [P1][est: 800 tokens] Add UI synergy copy that explains which hero abilities can apply Abyssal weakness and why support casts count.

## 2026-06-02 abyssal purple rune damage

- [P0][est: 900 tokens] Add a fourth Abyssal hero or rebalance the Abyssal 4 tier so the purple-rune bonus can activate from the MVP roster.
- [P0][est: 1000 tokens] Add combat-log events that show base purple-rune damage, Abyssal 4 bonus damage, and final resisted damage.
- [P0][est: 900 tokens] Add a rune-effect preview value that reflects active synergy modifiers before the player swaps.
- [P0][est: 900 tokens] Tune the Abyssal 4 purple-rune bonus against great-rune and chain multipliers.
- [P1][est: 800 tokens] Add UI synergy warnings when a tier exists but cannot currently be completed by the available roster.

## 2026-06-02 mechanist opening drone

- [P0][est: 1000 tokens] Add combat-log events for Mechanist 2 drone spawn, death, and damage contribution.
- [P0][est: 900 tokens] Add a portrait battle sprite or icon treatment for spawned drones so they are visually distinct from heroes.
- [P0][est: 1000 tokens] Move spawned-unit rules into typed summon definitions shared by Mechanist drones and summoner abilities.
- [P0][est: 900 tokens] Tune opening drone health, attack, and initial cooldown against early-round PvE encounters.
- [P1][est: 800 tokens] Add UI synergy copy that explains where the drone appears and what happens if the backline is full.

## 2026-06-02 mechanist match4 turret

- [P0][est: 1000 tokens] Add combat-log events for Mechanist 4 turret spawn, attacks, expiry, and skipped spawns.
- [P0][est: 900 tokens] Add a turret-specific portrait battle visual that clearly communicates temporary duration.
- [P0][est: 1000 tokens] Move temporary summons into a shared status/effect model instead of storing duration directly on `BattleUnit`.
- [P0][est: 900 tokens] Tune turret attack, health, cooldown, and lifetime against match-4 frequency.
- [P1][est: 800 tokens] Add a rune-board preview hint when a legal match-4 would create a turret for Mechanist teams.

## 2026-06-02 spirit dodge synergy

- [P0][est: 1000 tokens] Add combat-log events for Spirit 2 dodge attempts, successful dodges, and prevented damage.
- [P0][est: 900 tokens] Add a battle UI dodge flash or status marker so avoided attacks are readable.
- [P0][est: 1000 tokens] Replace deterministic dodge cadence with seeded combat RNG once the battle state carries a replayable random seed.
- [P0][est: 900 tokens] Tune Spirit 2 dodge chance against melee assassins and high attack-speed teams.
- [P1][est: 800 tokens] Add telemetry that reports effective damage prevented by dodge over a full battle.

## 2026-06-02 spirit white rune illusion

- [P0][est: 1000 tokens] Add combat-log events for Spirit 4 illusion source selection, spawn, attacks, and expiry.
- [P0][est: 900 tokens] Add a distinct translucent battle visual for illusions so they are not confused with real heroes.
- [P0][est: 1000 tokens] Replace deterministic illusion source selection with seeded combat RNG once replay-safe random state exists.
- [P0][est: 900 tokens] Tune illusion stat multiplier and duration against white-rune frequency and commander-energy gain.
- [P1][est: 800 tokens] Add UI synergy copy that explains illusions do not copy abilities in the MVP implementation.

## 2026-06-02 mage ability damage

- [P0][est: 1000 tokens] Add combat-log events showing base ability power, Mage 3 multiplier, defenses, and final damage.
- [P0][est: 900 tokens] Add UI stat previews for active ability damage after class synergy modifiers.
- [P0][est: 1000 tokens] Split ability scaling data by damage, heal, and shield effects so class modifiers cannot apply to the wrong effect type.
- [P0][est: 900 tokens] Tune Mage 3 damage against Abyssal purple-rune bonuses and late-round enemy resist.
- [P1][est: 800 tokens] Add balance snapshots comparing three-mage boards against non-mage caster-heavy boards.

## 2026-06-02 mage blue match4 charge

- [P0][est: 1000 tokens] Add combat-log events for Mage 5 blue match-4 bonus target selection, mana gained, and triggered casts.
- [P0][est: 900 tokens] Add a battle UI mana pulse that identifies the mage receiving the extra charge.
- [P0][est: 1000 tokens] Replace deterministic mage target selection with seeded combat RNG once replay-safe random state exists.
- [P0][est: 900 tokens] Tune bonus mana against caster mana caps so match-4 blue chains feel strong without forcing constant instant casts.
- [P1][est: 800 tokens] Add UI synergy copy that distinguishes normal blue rune mana from Mage 5 bonus charge.

## 2026-06-02 defender frontline health

- [P0][est: 900 tokens] Add UI stat previews that show Defender 2 frontline health before combat starts.
- [P0][est: 1000 tokens] Add combat-log or setup-log entries for position-based stat modifiers.
- [P0][est: 900 tokens] Tune Defender 2 health against Empire armor and yellow shield stacking.
- [P0][est: 1000 tokens] Add tests for moving a defender between frontline and backline during preparation before battle setup.
- [P1][est: 800 tokens] Add synergy panel copy that explains Defender 2 depends on tactical row position.

## 2026-06-02 defender yellow armor

- [P0][est: 900 tokens] Add a fourth Defender hero or rebalance the Defender 4 tier so yellow armor boosts can activate from the MVP roster.
- [P0][est: 1000 tokens] Add combat-log events for Defender 4 armor gained from yellow rune shields.
- [P0][est: 900 tokens] Add UI armor delta previews when a yellow rune match is selected by a Defender 4 team.
- [P0][est: 900 tokens] Tune Defender 4 flat armor gain against Empire 4 frontline shields and Defender 2 health.
- [P1][est: 800 tokens] Add synergy panel warnings when a class tier exists but the current roster cannot complete it.

## 2026-06-02 assassin backline strike

- [P0][est: 1000 tokens] Add a third assassin-class hero (or allow duplicate-counting) so the Assassin 3 backline strike can activate from a real MVP roster instead of synthetic synergy progress.
- [P0][est: 900 tokens] Add combat-log/visual events that highlight when an assassin redirects from the frontline to the enemy backline.
- [P0][est: 1000 tokens] Make melee assassins literally "jump" (teleport adjacency) to the backline target instead of pathing cell-by-cell, so the synergy reads instantly during combat.
- [P0][est: 900 tokens] Honor each hero's `Targeting` field ("backline_enemy", "farthest_enemy") in `SelectTargetIndex` so per-hero targeting and the Assassin synergy share one resolution path.
- [P1][est: 800 tokens] Tune assassin backline diving against enemy backline casters/healers to confirm it creates clutch moments without trivializing fights.

## 2026-06-02 assassin crit red-rune charge

- [P0][est: 1000 tokens] Surface `BattleState.PlayerRedRuneCharge` in the battle HUD so players see banked assassin-crit power before spending it on a red match.
- [P0][est: 900 tokens] Add combat-log events for each assassin crit (damage rolled, charge banked) and for the red rune effect that consumes the charge.
- [P0][est: 1000 tokens] Generalize the deterministic crit cadence to all heroes behind a feature flag once a replay-safe RNG exists, instead of only the Assassin 6 path.
- [P0][est: 900 tokens] Tune `AssassinCritRedRuneCharge` (currently 8) against red-rune base power so the bonus feels impactful without trivializing physical comps.
- [P1][est: 800 tokens] Decide and document whether banked red-rune charge should persist across rounds or reset each battle, then add a test pinning that behavior.

## 2026-06-02 commander model

- [P0][est: 1000 tokens] Wire `RunState` to start from a chosen `CommanderDefinition.CreateInitialState()` instead of the hardcoded `CommanderState.StoneOath`, and retire or remap the stray `stone_oath` default.
- [P0][est: 900 tokens] Add a `SelectCommander` run command plus a commander-selection screen that lists `CommanderCatalog.All` with passive, starting bonus, and recommended styles.
- [P0][est: 1000 tokens] Define and implement concrete `startingBonus` mechanics for the three commanders once the GDD specifies them, replacing the descriptive placeholder text.
- [P0][est: 900 tokens] Implement the three commander passives (Rune Archon extra blue rune, Warlord defender health, Alchemist chain-reaction gold) as triggered effects hooked into combat and round resolution.
- [P1][est: 800 tokens] Add a commander energy bar UI bound to `CommanderState.Energy/MaxEnergy` and surface white-rune/T-L energy gains feeding it.

## 2026-06-02 commander default alignment

- [P0][est: 800 tokens] Add a smoke assertion that every new run commander id exists in `CommanderCatalog.All`.
- [P0][est: 900 tokens] Add a catalog-level note in GDD or docs naming `rune_archon` as the MVP default commander.
- [P0][est: 900 tokens] Update save/load handling to reject or migrate legacy `stone_oath` snapshots once persistence stores commander ids.
- [P1][est: 800 tokens] Add UI copy that marks the default commander in the commander-selection list.
- [P1][est: 800 tokens] Add a debug command to start a run with each commander id for quick smoke testing.

## 2026-06-02 commander starting bonuses

- [P0][est: 900 tokens] Surface commander starting bonuses in the main menu and commander-selection UI using `StartingBonus.Description`.
- [P0][est: 1000 tokens] Add save/load coverage that preserves commander id and already-applied starting bonuses without reapplying them.
- [P0][est: 900 tokens] Tune Rune Archon's initial commander energy against white-rune and T/L energy gain frequency.
- [P0][est: 900 tokens] Tune Warlord's free `iron_guard` against starting shop economy and early bench pressure.
- [P1][est: 800 tokens] Add analytics counters for starting-bonus value gained during the first three MVP rounds.

## 2026-06-02 commander run selection

- [P0][est: 900 tokens] Add a presentation-layer commander picker that calls `RunState.NewRun(commanderId)` instead of always using the default commander.
- [P0][est: 900 tokens] Persist the selected commander id in run snapshots and verify it survives save/load round trips.
- [P0][est: 800 tokens] Add UI validation for unknown or locked commander ids before run creation.
- [P1][est: 800 tokens] Add a debug smoke loop that starts one run per `CommanderCatalog.All` entry and validates phase, bonus, and shop state.
- [P1][est: 700 tokens] Add analytics for commander pick rate once the menu exposes selection.

## 2026-06-02 commander energy bar

- [P0][est: 900 tokens] Bind `CommanderState.EnergyFillRatio` to a visible portrait HUD energy bar.
- [P0][est: 900 tokens] Add combat-log events for commander energy gained from white runes and T/L combos.
- [P0][est: 1000 tokens] Promote `CombatState.LastCommanderEnergyGain` from a last-swap value into a structured combat event feed for resumed UI feedback.
- [P0][est: 900 tokens] Tune white-rune energy gain against each commander's passive trigger cadence.
- [P1][est: 800 tokens] Add a debug overlay showing raw commander energy, max energy, and last gain per rune swap.

## 2026-06-02 rune archon passive

- [P0][est: 900 tokens] Add a battle UI flash when Rune Archon converts the third match-4 into a bonus blue rune.
- [P0][est: 1000 tokens] Replace `CombatState.LastBonusBlueRunesCreated` with a structured commander-passive event list.
- [P0][est: 900 tokens] Tune deterministic blue-rune placement so passive-created runes avoid unreadable immediate cascades unless intended.
- [P0][est: 900 tokens] Add save/load tests for `CommanderState.Match4CombosTowardPassive` across multiple rounds.
- [P1][est: 800 tokens] Add commander tooltip copy explaining that match-5/T-L combos do not count as match-4 triggers.

## 2026-06-02 warlord passive

- [P0][est: 900 tokens] Add a battle setup log entry identifying which defender received Warlord's +20% health.
- [P0][est: 900 tokens] Show Warlord's defender-health bonus in pre-combat stat previews.
- [P0][est: 1000 tokens] Generalize commander passives into a shared battle setup modifier pipeline instead of branching in `BattleState.Create`.
- [P0][est: 800 tokens] Tune Warlord's +20% health against Defender 2 frontline health and Empire armor stacking.
- [P1][est: 800 tokens] Add UI copy explaining how the "first defender" is selected when multiple defenders are deployed.

## 2026-06-02 alchemist passive

- [P0][est: 900 tokens] Show Alchemist's chain-reaction gold as a separate line on the reward screen.
- [P0][est: 900 tokens] Add combat-log events when the first chain reaction marks Alchemist's round bonus as earned.
- [P0][est: 800 tokens] Tune Alchemist's +1 chain gold against interest, streak, and chain-4 gold bonuses.
- [P0][est: 1000 tokens] Replace reward-time commander branching with a shared commander reward modifier pipeline.
- [P1][est: 800 tokens] Add telemetry for how often Alchemist earns passive gold in the first five rounds.

## 2026-06-02 starting gold

- [P0][est: 800 tokens] Surface starting gold in the preparation UI so the first shop budget is immediately visible.
- [P0][est: 900 tokens] Add economy balance tests for opening purchases with 5 gold and the Alchemist +2 start.
- [P0][est: 800 tokens] Update tutorial copy to explain the first five-gold shop decision once onboarding UI exists.
- [P1][est: 800 tokens] Track how often players spend all starting gold before round 1 combat.
- [P1][est: 900 tokens] Tune first-shop offers against the 5-gold start to avoid a dominant fixed opener.

## 2026-06-02 starting player level

- [P0][est: 800 tokens] Surface player level 1 in the preparation UI next to the field limit.
- [P0][est: 900 tokens] Add a level-up preview that shows the next hero cap before buying XP.
- [P0][est: 800 tokens] Add tutorial gating that explains why only two heroes can be fielded at level 1.
- [P1][est: 800 tokens] Track how often players buy XP before round 2.
- [P1][est: 900 tokens] Tune early XP thresholds against a level-1 start and 5-gold economy.

## 2026-06-02 starting run health

- [P0][est: 800 tokens] Surface the 20 run-health value in the main preparation HUD.
- [P0][est: 900 tokens] Add reward/defeat UI copy that explains run health loss after failed combat.
- [P0][est: 1000 tokens] Add tests for partial run-health damage across multiple losses.
- [P1][est: 900 tokens] Tune early round damage so a 20-health run survives expected tutorial mistakes.
- [P1][est: 800 tokens] Track run-health remaining after each MVP round for balance review.

## 2026-06-02 shop size by level

- [P0][est: 1000 tokens] Replace the deterministic default offer pool with level-aware random shop generation.
- [P0][est: 900 tokens] Show shop slot growth from 3 to 4 in the level-up UI preview.
- [P0][est: 900 tokens] Add tests for preserving shop size when loading a level 3+ preparation snapshot.
- [P1][est: 800 tokens] Track reroll frequency before and after unlocking the fourth shop slot.
- [P1][est: 900 tokens] Tune the fourth slot's impact on finding rare/epic heroes in rounds 4-6.

## 2026-06-02 six-slot bench

- [P0][est: 900 tokens] Render exactly six bench slots in the preparation UI with occupied/empty states.
- [P0][est: 900 tokens] Add drag/drop feedback when moving a fielded hero back to a full bench.
- [P0][est: 800 tokens] Add save/load coverage for full bench snapshots.
- [P1][est: 800 tokens] Track how often players hit the six-slot bench cap during early rounds.
- [P1][est: 900 tokens] Tune shop/reroll pressure so six bench slots feel constrained without blocking learning.

## 2026-06-02 shop hero purchase

- [P0][est: 900 tokens] Add preparation UI purchase buttons that disable when gold or bench space is insufficient.
- [P0][est: 900 tokens] Show a brief gold-spend animation and bench placement highlight after buying a shop hero.
- [P0][est: 800 tokens] Add tests for buying each rarity once rarity odds and multi-cost offers are generated.
- [P1][est: 800 tokens] Track first-round purchase patterns to tune opening shop offers.
- [P1][est: 900 tokens] Add undo/sell affordance guidance for accidental purchases during onboarding.

## 2026-06-02 shop reroll

- [P0][est: 900 tokens] Add a preparation UI reroll button that disables below 2 gold.
- [P0][est: 1000 tokens] Implement seeded shop roll generation so reroll produces fresh offers instead of caller-supplied deterministic lists.
- [P0][est: 800 tokens] Add reroll animation and preserve previous offers during the transition.
- [P1][est: 800 tokens] Track reroll count per round for economy tuning.
- [P1][est: 900 tokens] Add onboarding guidance warning that rerolling early can block XP purchases.

## 2026-06-02 xp purchase

- [P0][est: 900 tokens] Add a preparation UI XP purchase button that shows `4 gold -> 4 XP`.
- [P0][est: 900 tokens] Show progress toward the next level after each XP purchase.
- [P0][est: 800 tokens] Add tests for buying XP at each level threshold once level caps are finalized.
- [P1][est: 800 tokens] Track early XP purchases versus rerolls for economy tuning.
- [P1][est: 900 tokens] Add tutorial guidance explaining when buying XP unlocks another field slot.

## 2026-06-02 gold income formula

- [P0][est: 1000 tokens] Route round reward resolution through `EconomyConfig.CalculateGoldIncome` once win/loss streak state is stored on `RunState`.
- [P0][est: 900 tokens] Add reward-screen breakdown rows for base, win, streak, interest, and event income.
- [P0][est: 900 tokens] Add save/load coverage for win streak state before connecting income to run progression.
- [P1][est: 800 tokens] Track each income component separately for balance analysis.
- [P1][est: 900 tokens] Tune income pacing against reroll and XP purchase frequency.

## 2026-06-02 base income

- [P0][est: 900 tokens] Replace per-round static rewards with base income plus modifiers once reward flow carries battle result state.
- [P0][est: 800 tokens] Show the +3 base income row on the reward screen.
- [P0][est: 900 tokens] Add tests confirming losses still receive base income where GDD expects post-combat income.
- [P1][est: 800 tokens] Track average gold entering each round from base income.
- [P1][est: 900 tokens] Tune base income against fixed round rewards currently defined in `PveRunSchedule`.

## 2026-06-02 win income bonus

- [P0][est: 900 tokens] Store last combat result on reward flow so win bonus can be applied automatically.
- [P0][est: 800 tokens] Show the +1 win bonus as a separate reward-screen row.
- [P0][est: 900 tokens] Add tests confirming timer victories and enemy-elimination victories both count for win income.
- [P1][est: 800 tokens] Track win-bonus frequency across the 10-round MVP run.
- [P1][est: 900 tokens] Tune win bonus against streak bonus so early snowballing stays controlled.

## 2026-06-02 streak income bonus

- [P0][est: 1000 tokens] Add win-streak state to `RunState` and update it on combat victory/defeat.
- [P0][est: 900 tokens] Show current streak and next streak-income threshold in preparation UI.
- [P0][est: 900 tokens] Add reward-screen rows for +1 at three wins and +2 at five wins.
- [P1][est: 800 tokens] Track streak length distribution across MVP runs.
- [P1][est: 900 tokens] Tune streak thresholds if early perfect runs snowball too quickly.

## 2026-06-02 interest income bonus

- [P0][est: 900 tokens] Show current interest tier and next 10-gold threshold in preparation UI.
- [P0][est: 800 tokens] Add reward-screen rows for interest income and cap at +3.
- [P0][est: 900 tokens] Add tests for interest after purchases/rerolls once reward flow applies income automatically.
- [P1][est: 800 tokens] Track how often players reach each interest tier.
- [P1][est: 900 tokens] Tune interest cap if hoarding outperforms buying units in early rounds.

## 2026-06-02 event income modifier

- [P0][est: 900 tokens] Thread event reward choices into `CalculateGoldIncome(eventBonus)` during reward resolution.
- [P0][est: 900 tokens] Show event income or penalty as a separate reward-screen row.
- [P0][est: 800 tokens] Add tests for positive and negative event modifiers from concrete event definitions.
- [P1][est: 800 tokens] Track average event bonus impact per run.
- [P1][est: 900 tokens] Tune event penalties so they create risk without breaking early economy.

## 2026-06-02 player level thresholds

- [P0][est: 900 tokens] Show XP threshold progress for levels 1-5 in the preparation UI.
- [P0][est: 900 tokens] Add auto-level or repeated-level-up handling if players bank enough XP for multiple levels.
- [P0][est: 800 tokens] Add save/load tests for player level and banked XP at each threshold.
- [P1][est: 800 tokens] Track which round players usually reach each level.
- [P1][est: 900 tokens] Tune XP thresholds once round rewards and XP purchases are connected in UI.

## 2026-06-02 field hero limits

- [P0][est: 900 tokens] Show field cap `used/limit` in the preparation UI for levels 1-5.
- [P0][est: 900 tokens] Preview the next hero cap before buying XP or leveling up.
- [P0][est: 800 tokens] Add save/load tests for teams at each field cap.
- [P1][est: 800 tokens] Track how often players hit each field cap before leveling.
- [P1][est: 900 tokens] Tune hero limits against target 5-6 heroes by the final round.

## 2026-06-02 shop rarity odds

- [P0][est: 1200 tokens] Implement seeded shop generation that rolls rarities from `ShopRarityOdds` and then chooses matching heroes.
- [P0][est: 900 tokens] Show current level's rarity odds in the shop UI.
- [P0][est: 900 tokens] Add statistical smoke tests for seeded shop generation over large sample counts.
- [P1][est: 800 tokens] Track actual offered rarity distribution by player level.
- [P1][est: 900 tokens] Tune rarity odds against the 10-round MVP economy pace.

## 2026-06-02 run health damage formula

- [P0][est: 900 tokens] Route combat defeat resolution through `CalculateRunHealthDamage` with actual surviving enemy star totals.
- [P0][est: 800 tokens] Show run-health damage preview before confirming combat loss results.
- [P0][est: 900 tokens] Add tests for surviving enemy star counts from real enemy formations.
- [P1][est: 800 tokens] Track average run-health damage per failed round.
- [P1][est: 900 tokens] Tune run-health damage against the 20-health run length target.

## 2026-06-02 run damage floor

- [P0][est: 800 tokens] Add UI copy that displays the final integer run-health damage, not the raw formula.
- [P0][est: 900 tokens] Add tests for every MVP round's base floor contribution.
- [P0][est: 900 tokens] Expose raw and floored damage in a debug balance report.
- [P1][est: 800 tokens] Track player confusion around post-loss damage if preview values change after rounding.
- [P1][est: 900 tokens] Tune round scaling if floor behavior makes rounds 1-2 feel too similar.

## 2026-06-02 run damage clamp

- [P0][est: 900 tokens] Show min/max damage caps in debug balance tooling.
- [P0][est: 900 tokens] Add tests that real late-round enemy formations cannot exceed the 8-damage cap.
- [P0][est: 800 tokens] Add reward/defeat UI copy when damage was capped.
- [P1][est: 800 tokens] Track how often the 8-damage cap is reached.
- [P1][est: 900 tokens] Tune the cap if final-round losses feel too punishing for 20 run health.

## 2026-06-02 tutorial defeat protection

- [P0][est: 900 tokens] Add UI feedback when tutorial loss protection prevents run defeat.
- [P0][est: 900 tokens] Decide whether protected tutorial losses should grant full, reduced, or no round rewards.
- [P0][est: 800 tokens] Add save/load tests for protected tutorial combat defeat transitions.
- [P1][est: 800 tokens] Track how often players trigger tutorial defeat protection.
- [P1][est: 900 tokens] Tune round 1 enemies if protection triggers too frequently.

## 2026-06-02 pve run structure

- [P0][est: 900 tokens] Add round type metadata to each `PveRoundDefinition` for combat, event, shop, elite, boss, and final.
- [P0][est: 900 tokens] Add UI round-map data derived from the 10-round schedule.
- [P0][est: 800 tokens] Add tests that advancing from round 10 marks the run complete without requesting round 11.
- [P1][est: 800 tokens] Track round completion/drop-off rates through the 10-round MVP.
- [P1][est: 900 tokens] Tune rewards and difficulty once each scheduled round has real enemy formations.

## 2026-06-03 visual main menu

- [P0][est: 900 tokens] Persist the last selected commander and restore it before rebuilding the main menu.
- [P0][est: 900 tokens] Replace menu destination status text with actual level-select and settings screen rendering once those visual tasks land.
- [P0][est: 800 tokens] Add Unity play-mode assertions that the main menu contains New Run, Level Select, Settings, and selected commander UI nodes.
- [P0][est: 900 tokens] Add controller/touch navigation focus states for the three main menu buttons.
- [P1][est: 800 tokens] Add lightweight menu transition animation that keeps the commander card readable on small portrait screens.

## 2026-06-03 visual level select

- [P0][est: 1000 tokens] Replace compact level cards with reusable card view components before adding animated transitions.
- [P0][est: 900 tokens] Add per-round enemy portrait placeholders and threat icons keyed by `PveRoundType`.
- [P0][est: 900 tokens] Add touch-safe selected-card focus state before entering the preparation screen.
- [P0][est: 800 tokens] Add Unity play-mode coverage that verifies all 10 level cards render with type, reward, and status text.
- [P1][est: 900 tokens] Add a scroll or page mode if localized reward text overflows on smaller portrait devices.

## 2026-06-03 single-scene navigation

- [P0][est: 1000 tokens] Split the monolithic bootstrap screen rendering into dedicated view classes for menu, level select, preparation, combat, and rewards.
- [P0][est: 900 tokens] Add navigation button interactability tests in Unity play mode for menu -> level -> prep -> combat -> reward.
- [P0][est: 1000 tokens] Persist screen navigation state alongside `RunProgressSnapshot` so app suspend resumes the same visual screen.
- [P0][est: 900 tokens] Add a typed presentation event log for screen transitions and reward flow debugging.
- [P1][est: 800 tokens] Add controller and keyboard navigation maps for the single-scene flow before desktop QA.

## 2026-06-03 level intro transition

- [P0][est: 900 tokens] Move transition timing and fade opacity into a presentation tuning config.
- [P0][est: 900 tokens] Add per-round accent colors and iconography for tutorial, event, elite, boss, and final transitions.
- [P0][est: 800 tokens] Add a transition skip path for repeated QA runs and accessibility settings.
- [P0][est: 900 tokens] Add Unity play-mode coverage that waits for the intro coroutine and verifies preparation screen rendering afterward.
- [P1][est: 800 tokens] Add subtle audio and haptic hooks to level intro transitions once settings controls exist.

## 2026-06-03 preparation screen

- [P0][est: 1000 tokens] Bind preparation tactical cells to live `RunState.Team` and bench hero placement instead of demo units.
- [P0][est: 900 tokens] Add touch drag/drop handlers for bench-to-field placement and field-to-bench return.
- [P0][est: 900 tokens] Add disabled and affordability states for shop, reroll, XP, and fight controls.
- [P0][est: 800 tokens] Add Unity play-mode coverage for preparation screen gold, level, field cap, bench, shop, and fight button nodes.
- [P1][est: 900 tokens] Add compact hero detail popovers from bench and shop cards before full hero-detail screen work.

## 2026-06-02 ten-round GDD content

- [P0][est: 1200 tokens] Wire `HasCombat` into the run loop so event round 4 and shop round 9 skip combat and use a dedicated phase.
- [P0][est: 1000 tokens] Grant the structured `PveRoundReward` (starter hero, hero choice, artifact, rare artifact, free reroll) when a round resolves.
- [P0][est: 900 tokens] Show `EnemyName` and `DesignGoal` in the preparation enemy preview instead of the raw enemy id.
- [P1][est: 900 tokens] Build a level-select / round-map UI from `PveRunSchedule` using round type, reward, and difficulty tier.
- [P1][est: 800 tokens] Author real enemy formations per round id and tune `CombatRuneSeed` and gold against the difficulty tiers.

## 2026-06-02 app navigation model

- [P0][est: 1400 tokens] Bind `AppNavigationState` to `PortraitGameBootstrap` so uGUI shows/hides screen roots instead of building only the combat surface.
- [P0][est: 1000 tokens] Drive screen changes from `RunState.Phase` via `ScreenForPhase` so combat/reward screens follow the live run automatically.
- [P0][est: 900 tokens] Add a fade/title transition hook keyed off `NavigateTo` for the GDD level-entry animation (task 217).
- [P1][est: 900 tokens] Add save/load of `AppNavigationState.Current` so a resumed session reopens the right screen.
- [P1][est: 800 tokens] Add a back-stack (list) instead of a single `Previous` once nested screens (settings inside preparation) are needed.

## 2026-06-02 level select model

- [P0][est: 1400 tokens] Render `LevelSelectModel.Build(run)` as uGUI cards with completed/current/locked styling on the level-select screen.
- [P0][est: 900 tokens] Disable starting locked levels in the UI and route only the current card into preparation via `AppNavigationState`.
- [P1][est: 900 tokens] Add per-level icons per `PveRoundType` (tutorial, combat, event, elite, boss, shop, final).
- [P1][est: 800 tokens] Localize `LevelCard.RewardSummary` strings through a string table instead of inline Russian text.
- [P1][est: 800 tokens] Show the `DifficultyTier` band as a colored header grouping rounds 1-3, 4-6, 7-8, 9-10.

## 2026-06-03 tactical placement view-model

- [P0][est: 1300 tokens] Wire the shop cards to `RunState.BuyHero` so the bench fills from real purchases instead of the preparation demo roster seed.
- [P0][est: 1000 tokens] Add drag-and-drop (pointer down/up) placement on top of tap-to-select so bench->field feels native on iOS portrait.
- [P0][est: 900 tokens] Surface placement failures (field full, bench full) as a brief on-screen toast instead of silently swallowing the `InvalidOperationException`.
- [P1][est: 900 tokens] Reuse `TacticalPlacementModel` for an in-combat read-only board so the same cell-state coloring drives the battle screen tokens.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the CoreSmoke `TacticalPlacementModel` cases once the Unity Test Framework is installed.

## 2026-06-03 combat HUD view-model

- [P0][est: 1400 tokens] Drive a live battle countdown coroutine off `CombatHudModel`/`CombatState.AdvanceTimer` so the on-screen timer actually ticks during combat instead of showing a static duration.
- [P0][est: 1200 tokens] Replace the demo `Units` array feeding `BuildKeyHudUnits` with real `BattleState` units so the HUD shows live ally/enemy HP and MP.
- [P0][est: 900 tokens] Render the slowdown state visually (timer bar tint / "SLOW 70%" badge) when `CombatHudModel.IsSlowed` is true during large combos.
- [P1][est: 900 tokens] Add a timer progress bar under the timer pill bound to `CombatHudModel.TimerFraction` with a low-time warning color under 10 seconds.
- [P1][est: 800 tokens] Surface live rune-effect activations (last matched color and chain depth) in the effect strip from `CombatState` instead of the static rune chips.

## 2026-06-03 level-complete summary screen

- [P0][est: 1300 tokens] Replace the `LevelCombatSimulator` mirror enemy with a data-driven PvE roster from `PveRoundDefinition`/`PveRunSchedule` so the level-complete stats reflect the actual scheduled enemy, not the player's own comp.
- [P0][est: 1100 tokens] Drive the real combat screen off `LevelCombatSimulator`/`BattleState` so the same resolved battle feeds both the live fight and the level-complete summary instead of recomputing at reward time.
- [P1][est: 900 tokens] Persist the resolved `LevelCompleteModel` totals into `RunProgressSnapshot` so a reloaded run can re-open the last results screen without re-simulating.
- [P1][est: 900 tokens] Add per-hero MVP contribution (top damage/healing unit) to `LevelCompleteModel` by accumulating per-unit totals in `BattleState`, feeding the GDD "лучший герой" summary line.
- [P1][est: 800 tokens] Animate the six stat pills (count-up tween, victory/defeat color flash) on the level-complete screen for clearer post-combat feedback in portrait.

## 2026-06-03 level-to-level transition

- [P0][est: 1200 tokens] Drive run defeat in the presentation by calling `RunState.ApplyRunDamage` on a lost combat so the run can actually end on defeat instead of always claiming the reward.
- [P0][est: 1000 tokens] Persist `runState` through `RunProgressStore` on each screen change so a reopened web session resumes the run mid-progression instead of restarting from round 1.
- [P1][est: 900 tokens] Show a between-rounds shop/reward step (artifact or hero choice) before the next preparation, using `PveRoundReward` from the just-cleared round.
- [P1][est: 900 tokens] Replace the next-round demo bench seed with carried-over team/bench so progression feels continuous instead of re-seeding placeholders.
- [P1][est: 800 tokens] Animate the round counter increment on the level transition overlay (e.g. "ROUND 2 -> 3") so the player perceives the run advancing without a scene reload.

## 2026-06-03 run summary screen

- [P0][est: 1200 tokens] Track per-unit damage/healing on `BattleState` and feed it into `RunSummaryModel.BestHero` so the best hero reflects real combat contribution instead of the stars/cost heuristic.
- [P0][est: 1000 tokens] Add total run rewards (cumulative gold earned, artifacts collected, heroes recruited) to `RunSummaryModel` rather than only the final balances.
- [P1][est: 900 tokens] Add a "New run" button on the run-summary screen that calls `StartNewRunFromMenu` so the player can immediately replay after finishing.
- [P1][est: 900 tokens] Surface each team hero's faction/class synergy contribution on the summary roster rows using `SynergyState`.
- [P1][est: 800 tokens] Persist completed-run summaries into a lightweight account history (best round reached, wins) for the GDD metaprogression screen.

## 2026-06-03 data-driven PvE enemy roster

- [P0][est: 1200 tokens] Drive the live combat screen off `LevelCombatSimulator.ResolveRoundMatch` so the on-screen fight and the level-complete summary share one resolved battle against the round's authored roster instead of resolving twice.
- [P0][est: 1100 tokens] Feed `PveRoundDefinition.EnemyStarTotal` into `RunState.ApplyRunDamage` (`2 + roundNumber/3 + survivingEnemyStars`) so run-health damage scales with the real surviving enemy roster rather than a placeholder.
- [P1][est: 1000 tokens] Move the hard-coded `EnemyComposition` lists in `PveRunSchedule` into JSON/ScriptableObject content so designers can retune PvE encounters without recompiling core.
- [P1][est: 900 tokens] Give enemy rosters their own faction/class `SynergyModifiers` in `ResolveRoundMatch` (currently `BattleState.Create` is called with default enemy synergies) so themed encounters like Council of Three Factions actually field their trait bonuses.
- [P1][est: 800 tokens] Render the round's authored enemy composition in the preparation "next enemy preview" panel so the player can scout the upcoming roster instead of only seeing the enemy name.

## 2026-06-04 main-menu hub screens

- [P0][est: 1200 tokens] Persist `AccountProgress` and `SettingsModel` through a lightweight store (alongside `RunProgressStore`) and call `AccountProgress.WithRunRewards` at run end so account level, soft currency, and settings survive between sessions instead of resetting to defaults.
- [P0][est: 1000 tokens] Wire `SettingsModel.BattleSpeedMultiplier`, sound/music/vibration toggles, and `GraphicsQuality` into the actual combat tick, audio sources, and `QualitySettings` so the settings screen changes real behaviour rather than only persisting state.
- [P1][est: 1000 tokens] Add collection filtering/sorting controls (by faction, class, rune, rarity) to `HeroCollectionModel` and the collection screen so the 20-hero roster stays browsable as content grows.
- [P1][est: 900 tokens] Gate hero/commander unlocks behind `AccountProgress.AccountLevel` (data-driven unlock thresholds) so `UnlockedHeroes`/`UnlockedCommanders` become meaningful and the collection shows locked entries.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke checks for `AccountProgress`, `MainMenuModel`, `CommanderSelectModel`, `HeroCollectionModel`, and `SettingsModel` so the view-model logic is covered inside the Unity test runner too.

## 2026-06-04 preparation screen view-model

- [P0][est: 1100 tokens] Add a deterministic-seeded shop refresh service in core (level-based rarity odds -> concrete offers) so `OnRerollShop` produces a genuinely new shop instead of re-fetching the fixed `ShopState.ForPlayerLevel` pool, and route `AdvanceRound` through it too.
- [P0][est: 1000 tokens] Wire the preparation "В бой" button through `RunState.StartCombat` (and the live `LevelCombatSimulator`) instead of `ShowCombatScreen`, so starting a battle from prep transitions real combat state rather than the demo rune-board screen.
- [P1][est: 900 tokens] Add a hero-merge affordance to the bench: surface `RunState.MergeOneStarHeroes`/`MergeTwoStarHeroes` readiness on `PreparationBenchSlot` so the prep screen highlights when three copies can be combined.
- [P1][est: 900 tokens] Extend `PreparationScreenModel` with a "next breakpoint" hint per active synergy (heroes needed to reach the next tier) so the synergy indicators guide team-building, reusing `SynergyProgress.NextTier`.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke `PreparationScreenModel` checks (shop affordability, bench selection, synergy/enemy preview, action gating) so the prep view-model is covered inside the Unity test runner.

## 2026-06-04 combat screen view-model

- [P0][est: 1200 tokens] Drive a live `BattleState` on `RunState` during the combat phase (ticked alongside `CombatState`) so `CombatScreenModel.Build(run, battle)` reads one authoritative fight instead of the presentation layer resolving a separate battle for rendering and another for the level-complete summary.
- [P0][est: 1000 tokens] Capture the resolved `RuneEffect` list from each `SwapRunes`/`ApplyRuneEffects` step onto `CombatState` (e.g. `LastRuneEffects`) so `CombatScreenModel.ActiveRuneEffects` reflects the real last match instead of effects the caller must thread in by hand.
- [P1][est: 900 tokens] Add a display name to `BattleUnit` (or resolve hero/enemy ids back to `HeroCatalog` names) so `CombatFieldUnit` can show real hero names on the battlefield instead of falling back to the class label or raw unit id.
- [P1][est: 900 tokens] Surface a commander-energy bar and active-synergy chips on `CombatScreenModel` (reuse `CommanderState.EnergyFillRatio` and `SynergyCalculator`) so the combat screen shows the same build context the preparation screen does.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke `CombatScreenModel` checks (battlefield cell states, HP/mana bars, board hint highlighting, rune-effect chips, pause label) so the combat view-model is covered inside the Unity test runner too.

## 2026-06-04 reward screen view-model

- [P0][est: 1100 tokens] Actually apply the chosen `RewardArtifactOption` effects in combat/economy: give `ArtifactState` (or a parallel runtime record) a typed effect so picking "Кровавый Кубок" etc. changes real numbers, closing the gap between the reward UI and the P1 artifact-model task.
- [P0][est: 1000 tokens] Grant the hero reward for real on continue: when a round has `GrantsStarterHero`/`HeroChoice`, present an actual hero pick (3 offers) and add the chosen `HeroInstance` to the bench instead of only labelling "возможная награда героем" on the reward screen.
- [P1][est: 900 tokens] Feed the live combat bonus gold (chain-4 bonus, alchemist commander) into `RewardScreenModel.Build` once real combat drives the reward, so the gold breakdown shows the bonus line instead of always 0 from the freshly-started-then-claimed combat in `ResolveRoundReward`.
- [P1][est: 800 tokens] Add a confirm gate on the continue button when an artifact choice is pending but unselected, so players do not skip a free artifact reward by tapping continue too quickly.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke `RewardScreenModel`/`ArtifactCatalog` checks (gold breakdown, three distinct artifact offers, rare vs common pool, hero-reward and continue routing) so the reward view-model is covered inside the Unity test runner too.

## 2026-06-05 event screen view-model

- [P0][est: 1200 tokens] Implement the P1 event mechanics that actually apply an accepted `EventOption` to `RunState` (trade health for gold, add a cursed hero, set a one-battle faction buff, remove a hero and grant an artifact), closing the gap between the new event screen and the still-unchecked "P1 - Артефакты, события и награды" event tasks.
- [P0][est: 1000 tokens] Route the run into `RunPhase.Event` for round 4 (and round 9 if treated as an event) so `EventScreenModel.Build(run)` is reached in the real navigation flow instead of round 4 falling through the reward path; add an `AdvanceRound` path that exits the event phase after a choice.
- [P1][est: 900 tokens] Resolve the faction-boost event's target faction from the player's strongest active synergy (reuse `SynergyCalculator`) and store it on the applied `EventOption`/run so the buff names a concrete faction instead of the generic "ваша сильнейшая фракция" copy.
- [P1][est: 800 tokens] Model the cursed-free-hero curse as typed data (e.g. a `HeroCurse` record with stat penalties) and attach it to the granted `HeroInstance`, so the curse has real combat impact rather than only a label on the event card.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke `EventCatalog`/`EventScreenModel` checks (four archetypes present, deterministic round pick, accept/decline copy, non-event-round rejection) so the event view-model is covered inside the Unity test runner too.

## 2026-06-05 synergy panel view-model

- [P0][est: 1100 tokens] Render `SynergyPanelModel` in the Unity preparation/combat UI as a real synergy panel (faction/class chips coloured by `SynergyStrength`, next-breakpoint hints, candidate-hero suggestions), replacing the bare `ActiveSynergies` list on `PreparationScreenModel`.
- [P1][est: 1000 tokens] Filter `NextTierHeroes` to heroes actually obtainable now (current shop offers + bench) and rank them, so the "герои, которые закрывают следующий порог" hint points at heroes the player can field this round rather than the whole catalog.
- [P1][est: 900 tokens] Add a fourth `SynergyStrength` level (or a numeric ratio) so the panel can colour synergies that are one hero away from their next tier differently from those several heroes away, giving stronger visual guidance.
- [P1][est: 900 tokens] Surface `SynergyPanelModel` on `CombatScreenModel` too (read-only) so the player can see which synergies are live during the fight, reusing the same view-model the preparation panel uses.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke `SynergyPanelModel` checks (active factions/classes, next breakpoint + heroes-needed, candidate heroes, strength colours, ordering) so the synergy panel view-model is covered inside the Unity test runner too.

## 2026-06-05 run summary reward preview

- [P0][est: 1000 tokens] Wire the Unity run-summary screen to call `RunSummaryModel.Build(run, account)` and render the new `RunRewardSummary` (earned XP, soft currency, account levels, unlock notices), then apply `AccountProgress.WithRunRewards` once on screen entry so the preview and the persisted account stay in sync.
- [P1][est: 1000 tokens] Implement real content unlocks (commanders/artifacts behind account level) so `RunSummaryModel.Build(run, account)` reports concrete "Новых командиров/героев" notices instead of only account level-ups, closing the gap with the unchecked "P1 - Метапрогрессия" unlock tasks.
- [P1][est: 900 tokens] Track per-hero combat contribution (damage/healing/shielding) during battle and feed it into the best-hero pick on `RunSummaryModel`, replacing the stars-then-cost heuristic with the actual MVP of the run.
- [P1][est: 800 tokens] Persist `AccountProgress` between runs (extend the save store used for `RunState`) so meta XP, currency and unlocks accumulate across sessions rather than resetting each launch.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke run-summary reward checks (XP/currency preview, account levels gained, unlock notices, null guards) so the reward preview is covered inside the Unity test runner too.

## 2026-06-05 artifact definition model

- [P1][est: 1300 tokens] Apply artifact effects for real: add an artifact runtime resolver keyed on `ArtifactEffectKind`/`ArtifactTrigger` so picking e.g. "Печать Торговца" (economy/RoundEnd) actually changes gold and "Тлеющее Ядро" (rune/OnRuneMatch) changes rune damage, closing the still-unchecked "как модификаторы боя, экономики или рун" task.
- [P1][est: 900 tokens] Add typed effect magnitudes to `ArtifactDefinition` (e.g. an `Amount`/`Percent` field) instead of encoding numbers in the description text, so balance values are tunable in one place per the codex data rule.
- [P1][est: 900 tokens] Carry rarity (and effect/trigger) on `ArtifactState` so the run's owned-artifact list and the save snapshot retain the full model rather than only id+name, and the run-summary/main screens can show artifact rarity.
- [P1][est: 800 tokens] Group the reward-screen artifact offer by effect kind (or guarantee variety) so the three choices are not all the same category, making the "выбор одного из трёх" decision more meaningful.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke artifact-definition checks (15-20 artifacts, unique ids, rarity/effect/trigger integrity, pool sizes, reward/state projection) so the artifact model is covered inside the Unity test runner too.

## 2026-06-05 claim reward artifact

- [P1][est: 1100 tokens] Wire the Unity reward screen's three artifact cards to call `RunState.ClaimRewardArtifact(id)` on tap (disabling the other two once one is chosen via `RoundArtifactClaimed`), replacing any placeholder selection handler so the on-screen pick actually mutates the run.
- [P1][est: 1000 tokens] Apply the chosen artifact's effect at claim time (or at its `ArtifactTrigger`) so picking e.g. "Печать Торговца" changes gold income, closing the still-unchecked "артефакты как модификаторы боя, экономики или рун" task instead of only storing id+name.
- [P1][est: 900 tokens] Persist `RoundArtifactClaimed` in `RunProgressSnapshot` (and bump the snapshot version) so saving/reloading mid-reward-screen cannot let the player claim a second artifact for the same round.
- [P1][est: 800 tokens] Add an "artifact-or-gold" resolution for round 4's `ArtifactOrGold` reward so the event/reward path can take either the gold or one of the three artifacts, since `ClaimRewardArtifact` currently only fires in `RunPhase.Reward`.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke claim checks (three deterministic offers, single claim per round, case-insensitive id, rejection of unoffered ids, gold-only and non-reward-phase guards) so the artifact-claim flow is covered inside the Unity test runner too.

## 2026-06-05 economy artifact modifiers

- [P1][est: 1300 tokens] Implement the rune artifact modifiers slice: extend `ArtifactModifiers` with per-colour rune deltas (green healing, blue mana, yellow shield, red physical, purple debuff, white amplify) and feed them into `RuneEffect`/match-3 resolution, closing the decomposed rune subtask of "артефакты как модификаторы рун".
- [P1][est: 1400 tokens] Implement the combat artifact modifiers slice: aggregate frontline armor (`iron_banner`), ally attack speed (`swift_boots`), revive (`phoenix_feather`) and on-kill lifesteal (`soul_harvest`), and apply them in `CombatFormulas`/the battle simulator, closing the decomposed combat subtask.
- [P1][est: 900 tokens] Wire `greedy_idol` once `EconomyConfig.CalculateGoldIncome` is used in the live loop: pass an interest-cap bonus from `ArtifactModifiers` so the rare economy artifact raises the interest ceiling rather than being a no-op today.
- [P1][est: 800 tokens] Move artifact effect magnitudes onto `ArtifactDefinition` (typed amount fields) and have `ArtifactModifiers.From` read them by id, so the modifier table and the catalog description cannot drift apart and balance lives in one place.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke economy-modifier checks (merchant-seal round-end gold, apprentice-tome XP discount, stacking, neutral defaults, prep-screen cost) so the artifact economy slice is covered inside the Unity test runner too.

## 2026-06-05 claim reward hero

- [P1][est: 1100 tokens] Wire the Unity reward screen's hero card(s) to call `RunState.ClaimRewardHero(id)` and render the three `RewardHeroOption`s (portrait, rarity, cost, role), disabling the picks once `RoundHeroClaimed` is set, so the on-screen hero reward actually adds to the bench.
- [P1][est: 1000 tokens] Surface `RewardHeroOptions()` on `RewardScreenModel.Build` (alongside `ArtifactOptions`) so the reward view-model carries the concrete hero choices instead of only the `HeroRewardLabel` string, keeping the screen data-driven.
- [P1][est: 900 tokens] Persist `RoundHeroClaimed` (and `RoundArtifactClaimed`) in `RunProgressSnapshot` with a version bump so reloading mid-reward-screen cannot let the player claim a second hero/artifact for the same round.
- [P1][est: 800 tokens] Bias the hero-choice reward pool toward the player's active synergies/factions (reuse `SynergyCalculator`) so the three offered heroes are build-relevant rather than a flat seeded draw from the rarity pool.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke hero-claim checks (three deterministic offers, rarity/cost bounds, single claim per round, case-insensitive id, bench-full and non-reward-phase guards) so the hero-reward flow is covered inside the Unity test runner too.

## 2026-06-05 rune artifact modifiers

- [P1][est: 1400 tokens] Implement the remaining combat artifact modifiers slice (the next unchecked task): aggregate frontline armor (`iron_banner`), ally attack speed (`swift_boots`), longer-lived summons (`clockwork_heart`), revive (`phoenix_feather`) and on-kill lifesteal (`soul_harvest`) into an `ArtifactCombatModifiers` value applied in `BattleState.Create`, mirroring the new `ArtifactRuneModifiers` rune slice.
- [P1][est: 1100 tokens] Plumb `RunState.RuneModifiers` (and the future combat modifiers) through `LevelCombatSimulator.ResolveRoundMatch`/`BattleState.Create` so the run-level autobattle actually consumes the run's artifacts and synergies, since the simulator currently creates battles with neutral modifiers.
- [P1][est: 900 tokens] Model `prism_lens` ("white runes boost next color") as a real next-match amplifier instead of the MVP commander-energy multiplier, e.g. a one-shot white-charge field on `BattleState` consumed by the following coloured rune effect, so the artifact matches its GDD description.
- [P1][est: 800 tokens] Move the rune-bonus magnitudes off `ArtifactRuneModifiers` constants and onto typed fields on `ArtifactDefinition`, having `From` read them by id, so the catalog description, effect kind, and applied number cannot drift apart and balance lives in one place.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke rune-modifier checks (per-colour scaling, chain-conduit chain bonus, additive stacking, neutral default, null guards) so the rune artifact slice is covered inside the Unity test runner too.

## 2026-06-05 combat artifact stat modifiers

- [P1][est: 1400 tokens] Implement `phoenix_feather` revive: track per-side "revive available" on `BattleState`, detect the first ally that drops to zero in `Tick`, and respawn it at a fraction of max health once per battle, closing the decomposed revive subtask of the combat-artifact task.
- [P1][est: 1300 tokens] Implement `soul_harvest` on-kill heal: when an enemy dies during `Tick`/ability/rune resolution, restore a flat or percentage heal to living allies, modelled as the GDD "вампиризм" combat artifact, closing the decomposed on-kill subtask.
- [P1][est: 1200 tokens] Thread `RunState.CombatModifiers`, `RuneModifiers` and `SynergyModifiers.ForTeam(team)` through `LevelCombatSimulator.ResolveRoundMatch`/`ResolveMirrorMatch` into `BattleState.Create`/`ApplyRuneEffects` so the run-level autobattle actually consumes the player's artifacts and synergies instead of fighting with neutral modifiers.
- [P1][est: 900 tokens] Implement the remaining combat artifacts that only need start-of-combat tweaks — `crown_of_command` (commander energy gain multiplier) and `clockwork_heart` (summon-duration multiplier read by the drone/turret/illusion spawn code) — by extending `ArtifactCombatModifiers`, since they fit the existing Create-time pattern without cross-tick events.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke combat-modifier checks (frontline-only armor, ally-only attack speed, additive stacking, enemy-side exclusion, neutral default, null guards) so the start-of-combat artifact slice is covered inside the Unity test runner too.

## 2026-06-06 phoenix feather revive

- [P1][est: 1300 tokens] Implement `soul_harvest` on-kill heal (the next unchecked combat-artifact subtask): detect enemy units that drop to zero during `BattleState.Tick`/ability/rune resolution by diffing the pre-tick snapshot, and heal living allies a flat or percentage amount, mirroring the new phoenix revive's snapshot-diff death detection.
- [P1][est: 1200 tokens] Thread `RunState.CombatModifiers` (now carrying the phoenix revive count) through `LevelCombatSimulator.ResolveRoundMatch`/`ResolveMirrorMatch` into `BattleState.Create` so the run-level autobattle actually seeds revive charges from owned artifacts instead of fighting with neutral modifiers.
- [P1][est: 900 tokens] Surface the remaining phoenix revive charge on `CombatHudModel`/`CombatScreenModel` (e.g. a small "Перо Феникса" badge that empties when spent) so the player can read that a revive is still available during the fight.
- [P1][est: 800 tokens] Add a `BattleState` revive event/log entry (which unit, at what health) so the combat-complete summary and future battle-replay can attribute the comeback to the phoenix feather rather than silently restoring health.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke phoenix checks (one revive per charge, half-health restore, charge spent, no second revive, summoned units excluded, enemy side never revived, stacking feathers) so the revive slice is covered inside the Unity test runner too.

## 2026-06-06 soul harvest on-kill heal

- [P1][est: 900 tokens] Implement the remaining start-of-combat combat artifacts (the next unchecked subtask): `hunters_mark` (ranged bonus vs backline), `clockwork_heart` (summon-duration multiplier), `crown_of_command` (commander energy gain) and `guardian_aegis` (shield persistence) by extending `ArtifactCombatModifiers`, since they fit the existing Create-time pattern.
- [P1][est: 800 tokens] Let the soul-harvest heal target the most-wounded ally (or scale by missing health) instead of healing every living ally a flat amount, so a single kill is not diluted across a full team and the artifact rewards focused sustain.
- [P1][est: 800 tokens] Track per-battle soul-harvest healing on `BattleState` (e.g. roll it into `PlayerHealingDone` attribution) and surface it on the combat-complete summary so the player can see how much the artifact contributed.
- [P1][est: 700 tokens] Detect ability-cast kills for the soul harvest too (hero `PhysicalDamage`/`MagicDamage` abilities currently heal via Tick/rune paths only), so a finishing ability also triggers the per-kill heal for full coverage.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke soul-harvest checks (per-kill heal, multi-kill scaling, rune-kill trigger, neutral default, stacking, no self-heal overflow past max HP) so the on-kill slice is covered inside the Unity test runner too.

## 2026-06-06 remaining combat artifacts

- [P1][est: 1200 tokens] Thread `RunState.CombatModifiers`/`RuneModifiers`/synergies through `LevelCombatSimulator.ResolveRoundMatch` into `BattleState.Create`/`ApplyRuneEffects` (the next unchecked P1 task) so the run autobattle actually benefits from the now-implemented combat artifacts instead of fighting with neutral modifiers.
- [P2][est: 700 tokens] Surface the four new persistent combat multipliers (`hunters_mark`, `clockwork_heart`, `crown_of_command`, `guardian_aegis`) on the combat HUD/level-complete summary so players can see when an owned artifact is influencing the fight.
- [P2][est: 600 tokens] Extend `guardian_aegis` to also scale hero-ability shields (the `HeroAbilityKind.Shield` path currently uses the neutral default), threading the player shield multiplier through `TryCastAbility`/`ApplyHeroAbility`, so the artifact covers every shield source consistently.
- [P2][est: 600 tokens] Make `clockwork_heart` also extend the permanent Mechanist opening drone (e.g. grant it a long capped lifetime) or document explicitly that the artifact only affects timed summons, so its description "призванные юниты живут дольше" matches behaviour for all summon types.
- [P1][est: 800 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke coverage for the four combat artifacts (ranged backline boost vs frontline/melee gates, turret-lifetime extension, player-only commander energy scaling, shield strength with the 60% cap) so the slice is covered in the Unity test runner.

## 2026-06-06 run modifiers into round autobattle

- [P1][est: 900 tokens] Compute and carry an enemy commander for boss/elite rounds (currently `enemyCommander` is always null in `LevelCombatSimulator.RunToResolution`), so authored PvE bosses can use commander passives like the player does.
- [P1][est: 800 tokens] Add CoreSmoke behavioural coverage proving a run with a strong combat artifact (e.g. `iron_banner`/`swift_boots`) measurably improves the resolved round outcome vs the neutral run, not just that the modifiers are accepted, to lock in the integration.
- [P2][est: 700 tokens] Surface the active player synergies and owned combat artifacts on the level-complete summary so the player can see why the autobattle numbers changed after the modifiers were threaded in.
- [P1][est: 900 tokens] Drive the round autobattle with the player's actual match-3 moves (replaying recorded swaps/rune effects through `ApplyRuneEffect`) so `RunState.RuneModifiers` and match-3 skill influence the resolved result, closing the remaining rune-modifier subtask.
- [P2][est: 600 tokens] Cache `SynergyModifiers.ForTeam(team)` per resolution (it is currently evaluated once for the player and once via `BuildRoundEnemySynergies`) behind a small struct so repeated summary rebuilds for the same team/round avoid recomputing synergies.

## 2026-06-06 trade health for gold event

- [P1][est: 900 tokens] Implement the three remaining event archetypes (`AcceptCursedFreeHero`, `AcceptFactionBoost`, `AcceptSacrificeHeroForArtifact`) on `RunState`, reusing the new `EnterEvent`/`EnsureUnresolvedEvent`/`RoundEventResolved` scaffolding so all four GDD events resolve through one consistent flow.
- [P1][est: 700 tokens] Wire the Unity event screen in `PortraitGameBootstrap` to call `EnterEvent` on no-combat event rounds and route the accept/decline buttons to the matching `RunState` resolution method, since the core mechanic now exists but the presentation layer still has no path into the event phase.
- [P2][est: 600 tokens] Persist `RoundEventResolved` (and the event phase) through `RunProgressSnapshot` save/restore so a run interrupted on the event screen resumes in the correct unresolved-event state instead of falling back to preparation.
- [P2][est: 500 tokens] Make the merchant trade balance (`TradeHealthCost`/`TradeGoldReward`) scale with round depth or run health remaining, so the GDD "риск и награда" round 4 stays meaningful in later runs instead of being a flat fixed-value trade.
- [P1][est: 600 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke event-resolution checks (enter only on event rounds, advance blocked until resolved, trade spends health/grants gold, unsafe trade refused, decline no-op) so the event slice is covered inside the Unity test runner too.

## 2026-06-06 cursed free hero event

- [P2][est: 700 tokens] Surface the curse on the bench/preparation UI (an icon or tint on cursed `HeroInstance`s) so the player can tell a gifted hero is weakened, since the flag now exists but is invisible in the presentation layer.
- [P2][est: 600 tokens] Let the curse be lifted at a cost (e.g. a later shrine event or gold sink) by clearing `HeroInstance.Cursed`, giving the GDD risk/reward loop an escape valve instead of a permanent debuff.
- [P1][est: 700 tokens] Persist `HeroInstance.Cursed` through `RunProgressSnapshot` save/restore so a cursed gift survives leaving and resuming the run; verify nothing drops the flag when heroes move bench<->board or merge.
- [P2][est: 600 tokens] Decide and implement what happens to the curse on a star merge (cursed + clean copies): currently `MergeHeroes` keeps the survivor's flag, so document or normalise whether merging launders or spreads the curse.
- [P1][est: 600 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke cursed-gift checks (bench add, cursed/one-star, full-bench refusal, double-take refusal, stat penalty vs clean hero) so the slice is covered inside the Unity test runner.

## 2026-06-06 faction blessing event

- [P1][est: 800 tokens] Wire `RunState.PendingFactionBoost` into the Unity round-resolution call in `PortraitGameBootstrap` (pass it as `playerFactionBoost` to `ResolveRoundMatch`) so the blessing actually affects the presented autobattle, since the core now supports it but the bootstrap still resolves with the neutral default.
- [P2][est: 700 tokens] Offer the faction-blessing choice as a small picker of the factions the player currently fields (instead of a fixed event archetype), so the player selects which of their own factions to empower per the GDD intent.
- [P2][est: 600 tokens] Persist `PendingFactionBoostId` through `RunProgressSnapshot` save/restore so a blessing queued on the event screen survives a resume and is still spent on the following battle.
- [P2][est: 700 tokens] Make the faction blessing grant the faction's thematic effect (e.g. attack speed for Wild, magic damage for Abyssal) instead of a flat health/attack multiplier, so the blessing reads as a faction identity rather than generic stats.
- [P1][est: 700 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke faction-blessing checks (queue by id/name, reject unfielded/unknown factions, autobattle damage increase, consumption on resolve) so the slice is covered inside the Unity test runner.

## 2026-06-06 sacrifice hero for artifact event

- [P2][est: 700 tokens] Offer the sacrifice as a one-of-three artifact choice (reuse `ArtifactCatalog.OfferThree`) instead of always granting the first option, mirroring the reward-screen pick so the event is a real decision rather than a fixed swap.
- [P2][est: 600 tokens] Block or warn on sacrificing the run's last hero (the core lets the team/bench reach zero), so the player cannot trade themselves into an unwinnable next round.
- [P2][est: 600 tokens] Refund partial gold or scale the artifact rarity by the sacrificed hero's stars/cost, so giving up a 3-star carry feels distinct from dumping a spare 1-star.
- [P1][est: 700 tokens] Wire the Unity event screen to present the bench/board heroes as selectable sacrifice targets and call `AcceptSacrificeHeroForArtifact(instanceId)`, since the core mechanic now exists but no presentation path reaches it.
- [P1][est: 700 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke sacrifice checks (remove from bench, remove from board, grant deterministic relic, reject unknown hero, double-resolve refusal) so the slice is covered inside the Unity test runner.

## 2026-06-06 aggregate round reward calculation

- [P1][est: 700 tokens] Refactor `RewardScreenModel.Build` to consume a `RoundRewardBreakdown` instead of a single `bonusGold` int, splitting the "Бонус боя" line into labelled chain/alchemist/artifact rows so the reward screen shows the full composition the breakdown now computes.
- [P2][est: 600 tokens] Capture the `RoundRewardBreakdown` onto the run (or the reward screen state) at `ClaimReward` time so the reward screen can render the exact breakdown after combat clears, instead of re-deriving the bonus from a now-null combat state.
- [P2][est: 600 tokens] Fold the `ArtifactOrGold` round option into the breakdown as an explicit gold-vs-artifact branch (it currently only flags an artifact choice), so the player-facing "артефакт или золото" rounds compute the gold alternative in one place.
- [P2][est: 500 tokens] Add the free-reroll reward (`GrantsFreeReroll`) to the actual shop refresh on round advance, since the breakdown now reports it but `AdvanceRound` does not yet grant the reroll.
- [P1][est: 700 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke round-reward checks (base-only, chain bonus isolation, alchemist bonus isolation, default base gold, artifact-round offer flag, negative/null guards, ClaimReward == breakdown total) so the slice is covered in the Unity test runner.

## 2026-06-06 data-driven onboarding script

- [P1][est: 1100 tokens] Build the Unity interactive tutorial layer in `PortraitGameBootstrap` that consumes `OnboardingScript.ForRun(run)` to show the round's title/hint, highlight the relevant UI (shop, rune board, frontline cell) and gate progress, fulfilling the still-open "Сделать обучение интерактивным" task.
- [P2][est: 700 tokens] Add a "tutorial seen" set to `AccountProgress`/`RunProgressSnapshot` so each `OnboardingMechanic` is only taught once across runs and the settings "сброс обучения" toggle can clear it, matching the existing reset-tutorial setting.
- [P2][est: 600 tokens] Use `OnboardingScript.RevealedBy(round)` to progressively unlock/enable UI affordances (e.g. hide the event/artifact panels until their teaching round), so the first run literally opens mechanics one at a time instead of showing the full HUD immediately.
- [P2][est: 600 tokens] Localise the onboarding `Title`/`Hint` strings (and the rest of the Russian UI copy) through a string table keyed by `OnboardingMechanic`, so the hints are translatable rather than hard-coded, before more onboarding content is added.
- [P2][est: 500 tokens] Add a lightweight `OnboardingMechanic` reference on each tutorial `PveRoundDefinition` (or a CoreSmoke cross-check beyond DesignGoal text) so a future schedule edit that reorders the teaching rounds fails fast instead of silently desyncing the script.

## 2026-06-06 replay match-3 moves into the round autobattle

- [P1][est: 1000 tokens] Interleave the replayed match-3 rune moves across the autobattle ticks instead of front-loading them as a single opening burst, so timing (and clutch late-fight matches) matters; thread a per-move "elapsed at cast" timestamp from `PortraitGameBootstrap` into `ResolveRoundMatch` and apply each effect at its tick.
- [P1][est: 800 tokens] Pass `RunState.RuneModifiers` (and the recorded `roundRuneMoves`) into the live match-3 board feedback in `PortraitGameBootstrap`, so the on-screen rune strip and HUD preview the artifact-scaled power the round autobattle will apply, keeping the played board and the resolved outcome consistent.
- [P2][est: 700 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke replay checks (empty move list is a no-op, a burst adds player damage, a rune artifact scales it, a lethal burst wins the round) so the slice is covered inside the Unity test runner.
- [P2][est: 600 tokens] Record the replayed rune burst's contribution as its own labelled stat on `LevelCompleteModel` (e.g. "Урон рун"), so the summary screen distinguishes match-3 damage from autobattle damage instead of folding both into `PlayerDamageDealt`.
- [P2][est: 600 tokens] Cap or scale the replayed opening burst by the number of `runeMovesUsed` actually allowed per round (GDD target 4-8 moves), so a degenerate match-3 grind cannot dump an unbounded rune burst into the round and trivialise combat.

## 2026-06-07 account meta reward persistence

- [P1][est: 900 tokens] Back `AccountProgressStore` with a real cross-session backend (PlayerPrefs/JSON or a save file) so account XP, soft currency and unlocks survive an app restart, not just multiple runs in one session; add a `Version`-checked migration path like `RunProgressSnapshot`.
- [P1][est: 700 tokens] Add Unity edit-mode/play-mode tests that drive `PortraitGameBootstrap` through a run to the summary and assert the account XP/currency are applied exactly once and persisted, covering the headless verification gap left by this change.
- [P2][est: 600 tokens] Make soft currency spendable: wire a meta shop (rerolls, cosmetic unlocks, or starting-artifact picks) that debits `AccountProgress.SoftCurrency`, so the earned currency has a sink instead of only accumulating.
- [P2][est: 600 tokens] Implement the data-driven unlock model for commanders and starting artifacts (tasks 318/319): give each an `UnlockAccountLevel` and derive `UnlockedCommanders`/unlock notices from account level, keeping the MVP fully unlocked at level 1 to honour the no-pay-to-win rule.
- [P2][est: 500 tokens] Show the carried-forward account level/XP bar delta on the main menu after a finished run (animated gain) using the now-persisted `AccountProgress`, so meta progression is visible between runs rather than only on the summary screen.

## 2026-06-07 commander unlock schedule

- [P1][est: 900 tokens] Implement task 319 (разблокировка новых стартовых артефактов) as the artifact mirror of `CommanderUnlockSchedule`: add a starting-artifact selection step to `RunState.NewRun`/the prep flow and an `ArtifactUnlockSchedule` keyed by account level, then derive an `UnlockedStartingArtifacts` count on `AccountProgress` and a "Новых артефактов: N" notice in `RunSummaryModel` (the commander notice path is now proven and reusable).
- [P1][est: 700 tokens] Add Unity edit-mode/play-mode tests that drive `PortraitGameBootstrap` through the commander-select screen at account level 1 vs a leveled account, asserting locked cards are non-interactable, show the `UnlockHint`, and that `ConfirmCommanderSelection` falls back to the default for a locked id — covering the headless verification gap left by this change.
- [P2][est: 600 tokens] Persist and animate the commander-unlock moment: when `WithGains` crosses a `CommanderUnlockSchedule` threshold, surface a one-time "Открыт командир: <имя>" banner on the run-summary/main menu (using `NextCommanderUnlock`/`UnlockedCommanderIds` diffs) instead of the generic "Новых командиров: N" count.
- [P2][est: 600 tokens] Make `CommanderUnlockSchedule` data-driven from the same content source as `CommanderCatalog` (JSON/ScriptableObject) and add a CoreSmoke invariant that every catalog commander has exactly one schedule entry and the default unlocks at level 1, so adding a 4th commander cannot silently ship without an unlock level.
- [P2][est: 500 tokens] Show the next commander unlock as a progress hint on the main-menu account pill (e.g. "След. командир: ур. 2") driven by `AccountProgress.NextCommanderUnlock`, so players see why the locked commanders are greyed out before opening the selection screen.

## 2026-06-07 onboarding ability complexity and beginner synergy highlight

- [P1][est: 900 tokens] Drive the shop and reward pools from `AbilityComplexity`: at player level 1-2 (or the first run) bias `OfferRewardHeroes`/shop generation toward `HeroCatalog.SimpleAbilityHeroes` so a beginner only sees simple abilities until the onboarding script reaches the relevant teaching round, then relax the filter.
- [P1][est: 700 tokens] Surface `AbilityComplexity` on the hero detail screen (a small "простая/сложная способность" badge) and in `PreparationScreenModel`, so the existing data flag is visible to players and to the interactive tutorial layer that gates advanced heroes.
- [P1][est: 700 tokens] Wire `SynergyPanelModel.BeginnerHighlight`/`BeginnerHint` into `PortraitGameBootstrap` as a spotlight on the prep-screen synergy panel (pulse the highlighted row + show the hint) to close the documented Unity verification gap for "первые синергии должны быть очевидными".
- [P2][est: 600 tokens] Add Unity edit-mode tests mirroring the new CoreSmoke checks (every Common hero simple, round-1 reward only simple, `BeginnerHighlight` non-null for a started team and null for an empty team) so the onboarding guarantees are covered in the Unity test runner, not only headless.
- [P2][est: 500 tokens] Gate `BeginnerHighlight` behind the onboarding/account state so it only appears during the first run(s) (or until the player has manually completed a synergy), avoiding panel clutter for experienced players while keeping the first-synergy hint for newcomers.

## 2026-06-07 starting-artifact unlock schedule

- [P1][est: 900 tokens] Add a starting-artifact selection step to the run-start flow: extend `RunState.NewRun`/the prep screen to let the player pick one of their `AccountProgress.UnlockedStartingArtifactIds`, seed it into the run's relic collection, and gate locked choices with a `UnlockHint` mirroring the account-aware `CommanderSelectModel`.
- [P1][est: 700 tokens] Add a `StartingArtifactSelectModel` (mirror of `CommanderSelectModel`) that lists every `StartingArtifactUnlockSchedule` entry with locked/unlocked state + "Откроется на уровне аккаунта N" hints, and cover it with CoreSmoke checks at account level 1 vs a leveled account.
- [P1][est: 700 tokens] Add Unity edit-mode/play-mode tests that drive `PortraitGameBootstrap` through the run-summary at a level-crossing run and assert the "Новых стартовых артефактов: N" notice renders exactly once, closing the headless verification gap left by this change.
- [P2][est: 600 tokens] Make `StartingArtifactUnlockSchedule` data-driven from the same content source as `ArtifactCatalog` (JSON/ScriptableObject) and add a CoreSmoke invariant that every scheduled id is Common-rarity and the first entry unlocks at level 1, so adding a starting artifact cannot silently ship without an unlock level.
- [P2][est: 500 tokens] Surface the next starting-artifact unlock as a progress hint on the main-menu account pill (e.g. "След. артефакт: ур. N") driven by `AccountProgress.NextStartingArtifactUnlock`, matching the planned commander-unlock hint so meta progression toward both rosters is visible before opening a selection screen.

## 2026-06-07 cosmetic metaprogression rewards

- [P1][est: 900 tokens] Wire the unlocked cosmetics into `PortraitGameBootstrap`: apply the account's selected `BoardSkin` to the match-3 board surface and the `RuneEffect` to rune-match VFX, with a cosmetic-select screen that lists `AccountProgress.UnlockedCosmeticIds` and gates locked items with a "Откроется на уровне аккаунта N" hint (mirror the planned commander/artifact select).
- [P1][est: 700 tokens] Persist the player's chosen cosmetic per kind (selected board skin / rune effect / hero skin) on `AccountProgressSnapshot` so the equipped look survives between sessions, with a `Version`-checked migration like `RunProgressSnapshot` and a fallback to the level-1 default when a saved id is no longer unlocked.
- [P1][est: 700 tokens] Add Unity edit-mode/play-mode tests that drive a level-crossing run to the summary and assert the "Новой косметики: N" notice renders once and that the cosmetic-select screen marks locked items non-interactable — closing the headless verification gap left by this change.
- [P2][est: 600 tokens] Add a `CosmeticSelectModel` (mirror of `CommanderSelectModel`) returning per-kind cards with locked/unlocked state, `UnlockHint`, and the currently equipped id, plus CoreSmoke checks at account level 1 vs a leveled account; this is the reusable seam the bootstrap UI can bind to.
- [P2][est: 600 tokens] Extend the no-pay-to-win invariant into a single CoreSmoke "meta audit" that reflects over every unlock schedule (commanders, starting artifacts, cosmetics) and asserts: each entry is account-level gated, the level-1 set is non-empty, and no schedule references soft currency — so a future schedule that gates an unlock behind currency fails the build.

## 2026-06-08 MVP balance targets and feasibility projection

- [P1][est: 1000 tokens] Replace `BalanceProjection`'s lower-bound gold model with a deterministic full-run economy simulation (interest, win-streak bonuses, reroll spend, XP buys, hero merges) that reports the *expected* heroes-owned / 2-stars / active-synergies per round, then assert the realised curve lands inside `BalanceTargets` — turning the current feasibility check ("the goal is affordable") into an outcome check ("the goal is actually met by on-track play").
- [P1][est: 800 tokens] Track real match-3 move count and battle wall-clock during a battle (`BattleState`/combat HUD) and feed the per-battle totals back into a balance telemetry record, so the modeled `SecondsPerMatch3Move`/`ExpectedMatch3MovesInBattle` constants in `BalanceTargets` can be validated against actual play instead of an assumed cadence (closes the documented playtest gap for tasks 330/331).
- [P1][est: 700 tokens] Add Unity play-mode tests that drive `PortraitGameBootstrap` through rounds 3/5/8/10 with a scripted on-track purchase plan and assert the GDD milestones (3-4 owned, a 2-star, 1-2 synergies, 5-6 fielded) hold in the live game, covering the headless verification gap left by the projection-only checks.
- [P2][est: 600 tokens] Surface `BalanceProjection.Milestones()` in an editor balance-dashboard window (CSV/`Debug.Log` table of round, available gold, required gold, margin) so designers can see at a glance which milestone has the thinnest margin when they retune `EconomyConfig` or the `PveRunSchedule` gold payouts.
- [P2][est: 600 tokens] Implement the qualitative balance checks 332-335 (состав остаётся главным решением; match-3 влияет, но не подавляет автобой) as bounded-contribution invariants: cap and floor the share of total battle damage attributable to rune effects vs auto-attacks in a representative battle and assert both stay inside a configured band, giving those playtest-only tasks a headless regression guard.

## 2026-06-08 interactive onboarding gating

- [P0][est: 1200 tokens] Wire `OnboardingFlowModel` into `PortraitGameBootstrap`: render the active `ActionPrompt` as an overlay banner over the current screen and report the gated action (place hero, match red+blue, place tank front line, resolve event, trigger shield/heal rune, protect backline, win vs magic) into `OnboardingProgress.CompleteRound` so the prompt clears by doing, not by tapping through text.
- [P0][est: 900 tokens] Persist `OnboardingProgress` between sessions with a store mirroring `AccountProgressStore` (`OnboardingProgressSnapshot` of `CompletedGateIds`), so a returning player resumes the tutorial at the first uncleared gate instead of replaying it.
- [P1][est: 800 tokens] Auto-detect gate completion from real game state instead of explicit calls: derive `PlaceHeroOnField` from `RunState.Team.Count`, `PlaceTankInFrontline` from a defender on the front line, `ResolveRiskEvent` from `RoundEventResolved`, and rune gates from `CombatState` rune-clear events — so the Unity layer cannot forget to report an action.
- [P1][est: 700 tokens] Add a non-blocking "skip tutorial" affordance that marks all `OnboardingScript.AllGates` complete at once for returning players, and a settings toggle to replay it, gated so it never interrupts an in-progress run.
- [P2][est: 600 tokens] Add a soft fail-forward hint timer: if a player lingers on an active onboarding gate past N seconds, escalate `ActionPrompt` to a more explicit highlight (e.g. point at the shop or the tank slot) without forcing the action, keeping onboarding interactive but unstuck.

## 2026-06-08 portrait hero-match-3 combat layout

- [P0][est: 900 tokens] Wire live enemy damage into `EnemyHealthFraction()` so the top character's HP bar drains as the match-3 fight resolves into the autobattle, instead of the placeholder full bar — derive it from the replayed `roundRuneMoves`/`CombatState` enemy health so the stage reads as a real fight.
- [P1][est: 1000 tokens] Add a simple primitive "attack" animation when a red/magic match clears: flash the enemy character block and pop a floating damage number, keeping the auto-battler↔match-3 link visible (covers GDD "выделять важные события боя визуально").
- [P1][est: 800 tokens] Render the player's fielded squad as a small primitive row beneath the enemy character (mini ally tokens with HP), so the auto-battler side of the fight is shown on the combat screen too, not only on the preparation tactical grid.
- [P2][est: 700 tokens] Replace the placeholder enemy character square with a swappable `CharacterVisual` abstraction (primitive now, sprite/prefab later) keyed by enemy id, so the art pass drops in real boss visuals without touching the combat layout code.
- [P2][est: 600 tokens] Add a portrait safe-area pass: anchor the enemy stage / board / status / nav rows to fractions of the content frame instead of fixed pixel heights, so the hero-match-3 layout scales across iPhone SE→Pro Max aspect ratios without clipping the nav row.

## 2026-06-08 minimal portrait play screen

- [P0][est: 900 tokens] Add a slim in-board score/goal readout to the minimal play screen (e.g. enemy HP-to-zero progress) so the bare board+enemy view still communicates a win condition without bringing back the removed status panels.
- [P1][est: 1000 tokens] Make the play screen drive a real match-3→enemy-damage loop: each cleared red/magic match drains the top enemy's HP bar (`EnemyHealthFraction`) and ends the encounter at 0, turning the minimal screen into a complete mini-fight.
- [P1][est: 700 tokens] Gate the menu/run meta (commander select, shop, bench, tactical placement) behind a single small "MENU" entry from the play screen and keep them out of the default path, so the core match-3 stays the first and main screen while the auto-battler depth remains reachable.
- [P2][est: 600 tokens] Add an optional "details" toggle on the play screen that re-shows the retained `AddCombatStatusRow`/`AddRuneEffectStrip` panels for players who want the full HUD, so the minimal default and the detailed view share one code path instead of leaving those helpers dead.
- [P2][est: 700 tokens] Anchor the play screen (enemy / board / footer) to fractions of the content frame instead of fixed pixel heights and tile size, so the enlarged board scales to fill different portrait aspect ratios without manual height tuning.

## 2026-06-08 core-loop design-pillar verification

- [P0][est: 900 tokens] Replace `DesignPillarProjection`'s representative-average output model with a deterministic playthrough probe: run `LevelCombatSimulator.ResolveRoundMatch` for each of the 10 rounds and tally enemy damage attributable to the opening rune burst (`playerRuneMoves`) versus the autobattle, then assert the per-round match-3 share stays inside the dominance band — grounding the pillar in real fights, not a roster average.
- [P1][est: 800 tokens] Extend the pillar model with an "abilities" axis measurement: derive, from `CombatFormulas.ManaPerBlueRune` and representative `ManaMax`, how many extra ability casts a battle's blue-rune budget buys, and assert match-3 advances at least one cast earlier than pure autobattle — quantifying the GDD "match-3 влияет на способности" claim instead of only proving `ChargesHero`.
- [P1][est: 700 tokens] Add a clutch-moment guard: assert a great rune (×2.5) plus a chain-4 (×2.0) on a damage colour can swing a near-stalemate round in the simulator (flip a timer loss to a win) within the 4-8 move budget, capturing "clutch-моменты" as a behaviour test rather than a static energy check.
- [P2][est: 600 tokens] Surface the pillar metrics on a debug/balance screen in `PortraitGameBootstrap` (composition vs match-3 output share, covered levers) so a designer can see the auto-battler↔match-3 balance live and catch tuning that pushes match-3 past the dominance ceiling before it ships.
- [P2][est: 700 tokens] Make `DesignPillarProjection.RepresentativeMatchPower` data-driven from an authored "typical move mix" (e.g. 70% match-3, 20% match-4, 10% chain) instead of the flat base-match-3 constant, so the share model tracks realistic play and the dominance check tightens as match-3 tuning changes.

## 2026-06-08 battle readability model

- [P0][est: 900 tokens] Consume `BattleReadabilityModel.SelectVisibleEffects` in `PortraitGameBootstrap`'s combat rendering so the screen actually caps simultaneous rune-effect cues at `MaxSimultaneousEffects` and highlights `Critical` events, wiring the pure-data readability contract into the Unity layer instead of leaving it model-only.
- [P1][est: 800 tokens] Drive an explicit "important event" highlight (flash/scale/floating label) keyed off `BattleEventSalience`, so great-rune activations and T/L mass effects read as one emphasised beat — closing the still-open "Сделать бой визуально читаемым при одновременном автобое и match-3" task.
- [P1][est: 900 tokens] Add an adaptive-slowdown rule for "Уменьшить скорость боя, если игрок не успевает": when on-screen `Critical` event density over a short window exceeds a threshold (or the player is mid-chain), extend `CombatState`'s slowdown window, modeled in core and verified by smoke before any Unity work.
- [P2][est: 700 tokens] Promote `BattleReadabilityModel.MaxSimultaneousEffects` and the slowdown constants into a `SettingsModel`-backed "reduced motion / combat speed" option, so accessibility-minded players can tune visual density — pure-data first, Unity toggle later.
- [P2][est: 600 tokens] Build a headless attention-overload harness in `tools/CoreSmoke` that replays a worst-case chain (great rune + chain-4 + T/L) and asserts the surfaced cue count stays within budget every step, standing in for the "Провести быстрые игровые тесты на перегруз внимания" playtest until a Unity build exists.

## 2026-06-09 cross-zone battle attention

- [P0][est: 900 tokens] Wire `BattleAttentionModel` into `PortraitGameBootstrap`'s combat render: build `BattleCue`s from the live `BattleState` tactical events plus the last resolved `RuneEffect`s, then use `SelectVisibleCues`/`PrimaryFocus`/`ShouldDimOffFocusZone` to actually cap, focus and dim on screen — turning the pure-data contract into visible behaviour instead of model-only code.
- [P1][est: 800 tokens] Emit auto-battle `BattleCue`s from real tactical events (ability cast, unit death, heavy hit) inside `BattleState`/`LevelCombatSimulator` so the attention model is fed by genuine fight beats rather than presentation-layer guesses, with smoke coverage asserting a unit death produces a `Critical` auto-battle cue.
- [P1][est: 700 tokens] Couple `BattleAttentionModel.PrimaryFocus == Match3` with `CombatState`'s large-combo slowdown window so the camera/clock and the focus hint agree: when a deep-chain match-3 beat claims focus the clock slows, and verify the two triggers stay in lockstep in core before any Unity work.
- [P2][est: 700 tokens] Add a headless cross-zone overload harness in `tools/CoreSmoke` that replays a worst-case simultaneous beat (great rune + chain-4 on the board while an ally dies on the field) and asserts the surfaced cue count never exceeds `MaxSimultaneousCues` and a single zone owns focus each step — a stand-in for the "перегруз внимания" playtest until a Unity build exists.
- [P2][est: 600 tokens] Promote the focus decision into an accessibility-aware `SettingsModel` option (e.g. "always keep both zones visible" vs "dim off-focus zone") so reduced-motion players can opt out of zone dimming, keeping the rule data-driven in core with the Unity toggle layered on later.

## 2026-06-09 adaptive combat pacing

- [P0][est: 900 tokens] Apply `BattlePacingModel.RecommendedSlowdownMilliseconds`/`RecommendedSpeedPercent` inside `CombatState` (e.g. in `SwapRunes`/a new `ApplyAttentionLoad` step) so the adaptive slowdown actually drives the clock, and extend `CombatSpeedPercent` from its current binary slowed/normal into the three-tier (100/70/50) scale the model recommends — wiring the pure-data pacing into the live combat loop.
- [P1][est: 800 tokens] Feed `BattlePacingModel` from real simultaneous beats: gather the auto-battle `BattleCue`s and the last resolved `RuneEffect` cues each combat tick and recompute pacing, with a smoke test asserting a stacked beat (great rune + chain-4 + an ally death together) trips heavy overload and the 50% tier.
- [P1][est: 700 tokens] Add a player-responsiveness signal to "falling behind": combine attention overload with `SecondsSinceLastRuneSwap`/swap-cooldown pressure so the model distinguishes *overloaded* (slow down) from *idle/stuck* (show the match hint, don't slow), modeled in core and verified by smoke before any Unity work.
- [P2][est: 700 tokens] Surface the recommended speed tier on the combat HUD (`CombatHudModel.SpeedLabel`) as graded "SLOW 70% / SLOW 50%" states driven by `BattlePacingModel`, so the player reads why the clock eased instead of seeing a single generic slow flag.
- [P2][est: 600 tokens] Promote the pacing thresholds (`TrackableEventBudget`, `OverloadedCombatSpeedPercent`, `MaxAdaptiveSlowdownMilliseconds`) into a `SettingsModel`-backed "combat speed / reduced motion" preference so accessibility-minded players can bias toward slower pacing, keeping the rule data-driven in core with the Unity toggle layered later.

## 2026-06-09 attention-overload & onboarding playtests

- [P0][est: 900 tokens] Drive `AttentionOverloadPlaytest.Scenarios` from a live captured beat stream: have `PortraitGameBootstrap`/`BattleState` record the actual `BattleCue`s surfaced per combat tick during a session, then replay them through `AttentionOverloadPlaytest.Run` so the scripted playtest validates real fights instead of hand-authored cue sets — turning the regression suite into a true overload check once a Unity build exists.
- [P1][est: 800 tokens] Add an on-device overload telemetry hook: count frames where surfaced cues hit `MaxSimultaneousCues` and where `BattlePacingModel.IsPlayerFallingBehind` is true, log them to a session summary, and assert in core that a representative recorded battle stays under a configurable overload-frame budget — closing the documented live-playtest verification gap with data.
- [P1][est: 700 tokens] Extend `OnboardingPlaytest` with a time/effort budget per step (expected actions and a soft round-duration target mirroring the 45-75s battle window) so the difficulty check also guards pacing, not just one-mechanic-per-round, with smoke asserting no tutorial round demands more than a small bounded number of player actions.
- [P2][est: 700 tokens] Add a "stuck player" branch to `OnboardingPlaytest`: simulate a tester who fails to perform a gate, assert the flow keeps the same interactive prompt on screen (no soft-lock, no auto-skip) and that a hint escalates after the existing 8s idle window, verifying onboarding never strands a confused new player.
- [P2][est: 600 tokens] Surface both playtest verdicts (`AttentionOverloadPlaytest.AllReadable`, `OnboardingPlaytest.IsDifficultyFair`) plus their failing reasons in a small `tools/CoreSmoke` report section so a failing readability or onboarding regression prints the offending scenario name, making the scripted playtests actionable in CI rather than a single pass/fail.

## 2026-06-09 MVP boss content

- [P0][est: 900 tokens] Render the boss banner from `BossCatalog`: on entering a boss round, `PortraitGameBootstrap`/`PreparationScreenModel` should surface `BossDefinition.Name`/`Title`/`SignatureMechanic` so the player reads what the encounter checks before it starts, turning the pure-data identity into an on-screen telegraph.
- [P1][est: 800 tokens] Encode each boss's signature mechanic as data the combat layer can act on (e.g. a `BossModifier` enum: ArmorWall, SingleBigTarget, MixedCouncil) and apply a small, explicit stat tweak in `LevelCombatSimulator` so the elite actually plays as a damage sponge and the colossus as one big target — with smoke asserting the modifier matches the round composition shape.
- [P1][est: 700 tokens] Add a `BossEncounterModel` that, given the player's team, projects whether the team can clear each boss's design-goal check (sustained/magic damage vs the armor wall, focus damage vs the colossus) and flags a likely wall, mirroring the `BalanceProjection` pattern — a build-advisor for boss rounds, verified in core.
- [P2][est: 700 tokens] Extend `BossCatalog` with a distinct boss reward tier and wire it to `PveRoundReward` so beating a boss feels meaningfully better than a regular fight, with smoke asserting each boss round grants a bonus reward beyond base gold.
- [P2][est: 600 tokens] Add boss-specific rune-board pressure data (e.g. a recommended pre-fight board state or a telegraphed turn the player should save energy for) referenced from `BossDefinition`, so the match-3 layer also frames the boss as a set-piece rather than a stat check, kept data-driven in core.

## 2026-06-09 combat-round enemy variants

- [P0][est: 900 tokens] Let a fresh run pick from `EnemyVariantCatalog` per combat round (seeded by `RunState`), so repeat runs actually field different rosters; thread the chosen variant through `LevelCombatSimulator` instead of always using the canonical schedule, with smoke asserting a seeded pick is deterministic and stays within difficulty parity.
- [P1][est: 800 tokens] Add a second variant per combat round so each round has true rotation rather than a single alternate, keeping the star-total parity invariant and adding a smoke check that every combat round exposes at least two distinct compositions.
- [P1][est: 700 tokens] Extend `EnemyVariant` with a faction/synergy tag and assert in core that each variant exercises the same counter-lesson as its canonical round (e.g. round 7 variants are magic-damage heavy), so variety never quietly drops a teaching beat the onboarding relies on.
- [P2][est: 700 tokens] Generalise the parity check into a shared `EnemyCompositionRules` helper used by both `PveRunSchedule` and `EnemyVariantCatalog` (valid enemy-half placement, no stacked cells, star budget per round) so future content can be validated against one rule set instead of duplicated smoke assertions.
- [P2][est: 600 tokens] Surface the active variant name on the preparation screen's next-enemy preview (`PreparationScreenModel`) so the player sees they are facing "Теневая засада" rather than the default, making run-to-run variety legible, kept data-driven with the Unity label layered on later.

## 2026-06-09 additional elite encounters

- [P0][est: 900 tokens] Let the run insert an `EliteEncounterCatalog` pick into the elite slot (round 5, seeded by `RunState`) and thread it through `LevelCombatSimulator`, so elites actually rotate; smoke should assert the seeded pick is deterministic and stays at the elite star budget.
- [P1][est: 800 tokens] Add a second elite slot in later runs/difficulties (e.g. an extra elite around round 8 region) gated behind a future "ascension"/difficulty flag, drawing from `EliteEncounterCatalog`, keeping the data-driven contract and a smoke check that elites never collide with the boss rounds.
- [P1][est: 700 tokens] Tag each `EliteEncounter` with the build property it punishes (sustain/magic/armor) and add a `BalanceProjection`-style advisor that warns when the player's team has no answer to the drawn elite's twist, verified in core before any Unity work.
- [P2][est: 700 tokens] Unify `EliteEncounterCatalog`, `EnemyVariantCatalog` and `PveRunSchedule` placement/star validation into one shared `EnemyCompositionRules` helper so every content source is checked against the same enemy-half/no-stacking/star-budget rules instead of duplicated smoke assertions.
- [P2][est: 600 tokens] Give elites a distinct reward tier (better than regular combat, below the boss) wired through `PveRoundReward`, with smoke asserting an elite encounter grants a bonus beyond base gold, so the harder fight reads as worth the risk.

## 2026-06-09 expanded roguelite events (data-driven resolution)

- [P0][est: 900 tokens] Wire the run to surface and resolve every offered event through one UI path: add an `AcceptOfferedEvent(...)` dispatcher on `RunState` that routes by `OfferedEvent.Kind` (passing through the faction-id / hero-id only when the offered kind needs it), so the Unity event screen calls a single method instead of the per-archetype accepts, with smoke asserting the dispatcher applies the same deltas as each specific accept.
- [P1][est: 800 tokens] Add a second MVP event round later in the schedule (post-onboarding, e.g. around round 8 region) so the expanded `EventCatalog` pool actually surfaces in a normal run rather than only on alternate seeds; keep it no-combat and assert in core that its seed picks a different event than round 4 while preserving difficulty pacing.
- [P1][est: 700 tokens] Make the faction-blessing and cursed-hero magnitudes data-driven per option (carry `FactionBoostMultiplier`/`CurseStatMultiplier` on `EventOption`, store the pending multiplier on `RunState` instead of re-reading the catalog constant) so variant events can tune those numbers too, closing the remaining display==apply gap for the non-economy archetypes, verified in core.
- [P2][est: 700 tokens] Add a weighted/rarity-aware event picker (`EventScreenModel.PickForSeed` currently uniform `seed % count`) so positive windfalls stay rarer than risk/reward trades; encode weights as data on `EventOption` and smoke-assert the long-run distribution favours the intended archetypes.
- [P2][est: 600 tokens] Introduce an event that interacts with artifacts or synergies (e.g. "reforge an artifact" or "swap a rune affinity") to broaden roguelite decision space beyond pure economy, reusing the data-driven `OfferedEvent` resolution and `ArtifactCatalog`, kept in core with smoke for the new delta.

## 2026-06-09 main-screen cosmetics shop

- [P1][est: 800 tokens] Persist the player's `CosmeticLoadout` across runs by storing the equipped ids in the save layer (alongside `AccountProgress`), with a load/save round-trip smoke test, so an applied board/rune/hero look survives app restarts rather than resetting to default each session.
- [P1][est: 700 tokens] Thread the equipped `CosmeticLoadout` into the actual presentation surfaces (board skin into the match-3 board view, rune effect into match resolution VFX, hero skin into portraits) via a thin core selector, so equipping in the shop visibly changes the game, kept data-driven with a smoke check that the selector resolves each kind to the equipped id.
- [P2][est: 700 tokens] Add a soft-currency purchase path for an optional cosmetic tier (clearly separate from the level-unlock pool) so `SoftCurrency` becomes spendable on looks only — never power — with smoke asserting a purchase deducts currency, unlocks only a `CosmeticKind` reskin, and is rejected when the player cannot afford it.
- [P2][est: 600 tokens] Expand `CosmeticCatalog` with at least one more cosmetic per kind (currently 2 board / 2 rune / 1 hero) and extend `CosmeticUnlockSchedule` so every kind has real choice in the shop, keeping the one-entry-per-cosmetic invariant and the level-gated unlock curve, validated in core.
- [P2][est: 600 tokens] Add a "preview" projection to `CosmeticShopModel` (e.g. a `PreviewLoadout(cosmeticId)` that returns the loadout as it would look if equipped) so the Unity shop can show a before/after without mutating the live loadout, smoke-tested for locked cosmetics returning the unchanged loadout.

## 2026-06-09 in-combat battle speed-up button

- [P1][est: 800 tokens] Wire the player's `SettingsModel.BattleSpeed` through `CombatScreenModel.Build` and the Unity combat HUD so the in-combat `BattleSpeedButtonModel` reflects and toggles the real setting (tap cycles `BattleSpeedOptions.Next` and persists), making the button functional rather than display-only, with a smoke check that the screen model carries the chosen speed.
- [P1][est: 700 tokens] Apply `BattleSpeedMultiplier` to the actual autobattle tick in `LevelCombatSimulator`/the Unity tick loop (scale elapsed time fed to `CombatState.AdvanceTimer`) and add a smoke test that a fast-speed battle advances the same simulation in fewer real ticks while producing an identical outcome, proving speed-up changes pace not result.
- [P2][est: 700 tokens] Add a third speed tier (e.g. `BattleSpeed.Turbo` at 2.0x) to `BattleSpeedOptions` and the cycle, gated as a later-version/meta unlock, keeping the single-source-of-truth contract and extending the cycle/label smoke coverage.
- [P2][est: 600 tokens] Gate the in-combat speed-up button behind a "later version" unlock signal (e.g. tutorial completed or account level threshold) exposed as a `bool SpeedButtonAvailable` on the HUD model, with smoke asserting a fresh/tutorial run hides it and a progressed account shows it, honouring the "для поздних версий" intent.
- [P2][est: 600 tokens] Auto-suspend the speed-up during readability slowdowns: have the HUD report an effective multiplier that drops to 1x while `IsSlowed` so big combos stay readable even at fast speed, smoke-tested that effective speed = base during a large-combo slowdown regardless of the chosen tier.

## 2026-06-09 async PvP arena foundation (schema, save, matchmaking)

- [P1][est: 900 tokens] Add the replay step the design defers: an `ArenaBattleBuilder` that mirrors an `ArenaCompositionSnapshot` onto the enemy half (`row -> Rows-1-row`), rebuilds `HeroInstance`s from `HeroId`+`Stars` via `HeroCatalog`, applies the owner's `CommanderId`/`ArtifactIds`, and produces a `BattleState` the existing autobattle loop fights, with smoke asserting a captured snapshot round-trips into a fightable enemy board with matching positions.
- [P1][est: 700 tokens] Introduce an `ArenaRatingRules.Update(playerRating, opponentRating, won)` Elo-style step (the schema already carries `Rating`) so wins/losses move the matchmaking number; keep it pure in core with smoke asserting symmetric gain/loss, a bounded K-factor, and that rating never goes negative.
- [P1][est: 700 tokens] Add a small bundled `ArenaSeedPool` of hand-authored `ArenaCompositionSnapshot`s (valid placements, varied ratings/commanders) so async PvP has opponents before any backend exists; smoke-assert every seed snapshot is structurally valid and that `ArenaMatchmaker.FindOpponent` resolves a fair match across the seeded rating spread.
- [P2][est: 800 tokens] Wire capture into the run lifecycle: on a won PvE run, call `ArenaSnapshotBuilder.Capture` with the final board and persist the snapshot alongside `AccountProgress` in the save layer, with a load/save round-trip smoke test so a player's recorded composition survives app restarts and can be served to others later.
- [P2][est: 600 tokens] Add an `ArenaScreenModel` projection (player rating, chosen opponent's `OwnerName`/`Rating`/commander, hero count) built purely from the matchmaker result so the Unity arena screen renders the matchup without owning any selection logic, smoke-tested that it reflects the `FindOpponent` choice and reports "no opponent" for an empty pool.

## 2026-06-10 Live PvP lobby foundation (design, parallel rounds, health loss)

- [P1][est: 900 tokens] Add the per-player fight bridge the design defers: a `LivePvpRoundResolver` that, given each alive participant's board/opponent pairing, runs the existing `BattleState` autobattle and produces the round's `LivePvpRoundOutcome` set (winner = survivor, loser damage scaled by surviving enemy units), so `LivePvpMatch.ResolveRound` is fed by real combat instead of hand-written outcomes — smoke-tested that a deterministic board pairing yields the expected win/loss and damage.
- [P1][est: 700 tokens] Add `LivePvpPairing` to decide who fights whom each parallel round (round-robin with a ghost/clone fallback for odd alive counts, the standard auto-battler rule) as a pure function over the alive set, with smoke asserting no player fights themselves, pairings are stable/deterministic per round, and an odd alive count still produces a full pairing.
- [P1][est: 700 tokens] Make loss damage data-driven via `LivePvpConfig`: add a `DamageFor(round, survivingEnemyUnits)` curve (base-by-stage + per-surviving-unit, like TFT) so health loss escalates over a match, keeping the lobby agnostic but the numbers in one config; smoke-assert early rounds cost less than late rounds and more survivors hurt more.
- [P2][est: 700 tokens] Add a `LivePvpScreenModel` projection (each seat's name, health bar, alive/eliminated, current placement, round number, your-next-opponent) built purely from `LivePvpMatch` so the Unity lobby/standings UI renders without owning rules, smoke-tested that it reflects mid-match state and final standings best-first.
- [P2][est: 600 tokens] Add post-match meta rewards for Live PvP placement (account XP / soft currency by finish position, never power) routed through `AccountProgress`, with smoke asserting first place earns strictly more than last and that no reward grants combat advantage, honouring the non-pay-to-win rule.

## 2026-06-10 monetization design + non-pay-to-win policy

- [P1][est: 800 tokens] Fold the five non-loadout monetization formats (battle pass, commander portraits, emotes, finishers, progress boost) into a real owned/equipped state alongside the existing `CosmeticLoadout` — e.g. a `MonetizationInventory` tracking owned format ids per kind — so the account can actually hold and apply them, smoke-tested that ownership grants no numeric field and equipping changes only presentation.
- [P1][est: 900 tokens] Design and add a `BattlePassTrack` core model (tiers, free vs paid lane, cosmetic reward per tier drawn from `CosmeticCatalog`/monetization formats, XP-to-tier curve) with smoke asserting every paid-lane reward is cosmetic-only and that tier progression never grants gold/power, turning the battle-pass design doc into testable data.
- [P2][est: 700 tokens] Add a `MonetizationOfferModel` presentation projection (price display string, owned/equipped state, "cosmetic only" badge) built purely from `MonetizationCatalog` + account state, so a future store screen renders offers without owning policy, smoke-tested that it never surfaces a prohibited format and flags each offer cosmetic-only.
- [P2][est: 700 tokens] Extend `MonetizationPolicy.IsCosmeticOnly` to also walk the format's `AppliesTo` cosmetic surface and assert the linked `CosmeticDefinition`/`CosmeticKind` carries no power, giving the guard transitive teeth across the cosmetic pipeline, with a smoke test that a deliberately power-bearing fake cosmetic is rejected.
- [P2][est: 600 tokens] Add an `AccountProgress` cosmetic-progress channel (separate from account XP) that `CosmeticProgressBoost` may accelerate, proving the "convenience" format speeds only cosmetics: smoke asserts boosting advances the cosmetic channel but leaves `UnlockedCommanders`/`UnlockedStartingArtifacts` and run rewards untouched.

## 2026-06-11 async PvP rating step (Elo update)

- [P1][est: 700 tokens] Persist the updated rating back onto the player's stored `ArenaCompositionSnapshot` after a match: add a pure `snapshot with { Rating = ArenaRatingRules.Update(...) }` helper (e.g. `ArenaSnapshotBuilder.WithUpdatedRating`) plus a save round-trip so a player's recorded composition carries its post-match rating to the next matchmaking pass, smoke-tested that a win raises the stored snapshot's `Rating` and a loss lowers it.
- [P1][est: 600 tokens] Add a provisional-rating / placement-match phase: a higher K-factor for a player's first N arena matches (uncertainty period) that decays to `DefaultKFactor`, exposed as `ArenaRatingRules.KFactorForMatchCount(matchCount)`, so new players converge to their true rating fast without late-season volatility; smoke-assert early matches move more than settled ones and the K-factor never exceeds `MaxKFactor`.
- [P2][est: 700 tokens] Add an `ArenaLadderTier` projection (Bronze/Silver/Gold/… bands over a rating value) as pure data so the arena screen can show a rank badge from `Rating` alone, smoke-tested that band boundaries are contiguous, monotonic, and that a rating of 0 maps to the lowest tier.
- [P2][est: 600 tokens] Track a lightweight arena win/loss record on `AccountProgress` (wins, losses, current/best rating) updated alongside `ArenaRatingRules.Update`, with a save round-trip smoke test, so the player has a visible arena history without any backend — strictly cosmetic/statistical, granting no combat power (honours non-pay-to-win).
- [P2][est: 600 tokens] Add an `ArenaMatchResultModel` presentation projection (old rating, new rating, delta sign/magnitude, opponent name) built purely from a pre/post `ArenaRatingRules.Update` pair so the post-match screen renders the rating swing without owning Elo math, smoke-tested that the delta sign matches win/loss and the displayed new rating equals the rules output.

## 2026-06-12 persist async PvP rating onto stored snapshot

- [P1][est: 600 tokens] Add an `ArenaRatingRules.KFactorForMatchCount(matchCount)` provisional/placement curve (higher K for a player's first N arena matches, decaying to `DefaultKFactor`) and thread it through `ArenaSnapshotBuilder.WithUpdatedRating` so new compositions converge fast without late-season volatility; smoke-assert early matches move the stored rating more than settled ones and K never exceeds `MaxKFactor`.
- [P1][est: 700 tokens] Add a paired `ArenaSnapshotBuilder.WithUpdatedRatings(playerSnapshot, opponentSnapshot, playerWon, kFactor)` that returns both post-match snapshots in one call (winner up, loser down) so a recorded head-to-head settles symmetrically in a single transform; smoke-assert the total rating across both snapshots is conserved at equal ratings and each side stays clamped at zero.
- [P2][est: 700 tokens] Add an `ArenaMatchResultModel` presentation projection (old rating, new rating, signed delta, opponent name) built purely from a pre/post `WithUpdatedRating` pair so the post-match screen renders the swing without owning Elo math; smoke-assert the delta sign matches win/loss and the model's new rating equals the snapshot's.
- [P2][est: 600 tokens] Add an `ArenaLadderTier` band projection (Bronze/Silver/Gold/… over a rating value) as pure data so the arena screen shows a rank badge from a snapshot's `Rating` alone; smoke-assert band boundaries are contiguous, monotonic, and that rating 0 maps to the lowest tier.
- [P2][est: 600 tokens] Track a lightweight arena record on `AccountProgress` (wins, losses, current/best rating) updated alongside `WithUpdatedRating`, with a save round-trip smoke test, so the player sees arena history without a backend — strictly statistical, granting no combat power (honours non-pay-to-win).
